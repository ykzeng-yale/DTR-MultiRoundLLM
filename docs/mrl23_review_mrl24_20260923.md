# MRL-23 partial delivery reviewed; MRL-24 source/mock repair

Lead decision, 23 September 2026. Reviewed worker commit **18797b7ab593bd569522ad35a905d640a67b22cb**, published 15:54:09 UTC. This is the last-seen and independently reviewed **source** revision; it is not a new empirical-result revision.

**Decision: accept the measurement content as partial preparation; REPAIR the connected path and protocol; HOLD instrument and model execution.** MRL-24 below permits one bounded source/mock repair by the existing worker. It does not reopen the expired MRL-23 allowance or release any of the future 130 model calls / 66,560 reserved completion tokens / 228 isolated starts.

## Recovery and evidence states

The committed [recovery record](../results/mrl23_recovery_status_20260923.json) acknowledges the original decision, the recovery request and lead work through `5ae90187f1ad0cbb6199b5c32cd6bae05bab2b9b`. It corrects the processed decision SHA to `be0507ed328f91c646de91c3f72d7e9ff1829de4`. Publication and recovery receipt are now verified. The nine earlier missing-update reviews remain historical facts; the current consecutive-missing count resets to zero. Do not repost the old escalation or ask the owner again for a locator.

The worker identifies its existing Claude Code desktop session as `becb2798-9298-4306-8e12-77f81520493f` (local reference `8864ab`). A narrow lookup in the local Claude project records found no matching transcript on this host; no unrelated session was contacted. GitHub files remain the verified handoff. The worker reports an app shutdown around 08:20 UTC, restart around 15:00 UTC, last actual scheduler tick 08:10:19 UTC, and no active run/lease at publication. These are worker reports, not independently observed process history or current scheduler health.

The original cap was 08:41:27 UTC. The worker discloses one new static hash command at 14:59:25 UTC after that cap; this is outside the allowance and is not retroactively authorized. Four concurrent builder agents do not by themselves prove four simultaneous local CPU workloads, but the one-worker resource condition has not been independently reconciled. Return any existing command/time ledger that resolves this, without recreating missing history. Future MRL-24 local execution must be sequential as specified below. Honest late publication allows review; it is not timely completion or resource compliance.

## What was independently checked

Three read-only reviewers examined measurement, implementation and mathematical claims. The lead verified all twelve recovery artifact hashes and ran the focused builder checks: **52 passed, two failed** in 0.18 seconds of test time (0.407 seconds command wall, 0.342 seconds child CPU). Both failures concern byte-for-byte rebuilds. The raw source digest is unchanged: the builder serializes its locally resolved cache path, so this host's identical-byte fallback changes `CHANGE_NOTES.md` and the manifest. This is a portability defect, not evidence of changed task data. The worker's reported 1,556-test pass was not rerun or substituted for this result.

The lead also reproduced three failures with injected fakes: nominal collection reports a zero quality contrast without invoking a private scorer; a thrown transport exception escapes without returning the assignment record; and a fault on the second initial call still allows a public-check invocation on the first answer. The [independent review record](../results/e14_partial_independent_review_20260923.json) preserves the hash checks, exact rebuild differences, both audit-command timings and these reproductions. There were 133 fake transport invocations, zero study model calls/tokens, zero reference/control/candidate/public-check/containment program executions and $0 paid cost. These are source/mock checks, not efficacy or runtime instrument evidence.

The ten-root order, ten public examples, 37 private assertions (20 retained and 17 added), ten references and twenty controls match the lead specification. The required integer-square-root, iterative LCS and integer-division reference repairs are accepted by source reasoning. Public contents are clean on inspection. Predicted control failures and static validity still need a separately released isolated validation; finite tests do not establish semantic completeness. No task hold, backfill or endpoint redesign follows from the current review.

## Binding repair criteria

1. **Keep collection and quality separate.** `e14_mock_path.py` currently substitutes completed transports for private success, and `score_private` never calls its scorer. Preserve immutable request, answer and diagnostic artifacts, not just hashes. After collection closes, an injected mock scorer must consume those exact bound artifacts and attach the frozen endpoint's binary scores or unavailable-grade state. An observed score of zero is not missing. Suppress the primary point contrast if any of the 120 primary grades is unavailable, even when every response was delivered. Transport completion may be reported separately; it cannot stand in for correctness.

   With 60 assigned continuations per arm, let `S_D,S_N` be observed private successes and `M_D,M_N` unavailable grades. The all-assigned finite-score contrast lies in **[(S_D-S_N-M_N)/60, (S_D+M_D-S_N)/60]** without a missing-at-random assumption. These are completion bounds, not a confidence interval for conditional response means. A complete fixture with `S_D=30,S_N=18` must give 0.2. A fixture with those successes and `M_D=2,M_N=3` must suppress the point contrast and give [0.15, 14/60]. An all-missing directed arm and a fully graded neutral arm with 18 successes gives [-0.3, 0.7]. Test mixed zero/one grades and missing grades independently of transport status.

2. **Retain failures at the actual entry point.** Persist all 130 planned slots before dispatch; record attempts before invoking transport/checker boundaries. Thrown exceptions and uncertain attempts must retain the plan, partial outputs, attempted/unknown usage and explicit remaining states. No retries, missing-at-random assumption or silent resumption. An integrity or receiver-law fault must stop subsequent transport and public-check calls, including diagnostics for earlier answers. Test failed initials (their twelve branches remain missing), thrown initial and continuation calls, thrown checkers, incomplete diagnostics, mid-root cap exhaustion, all-missing arms, a fault after one successful initial, and explicit resume refusal. Exercise the entry point that writes the partial record, not only pure helper functions.

3. **Bind one actual release and adapter schema.** The mock reconstructs different public-context bytes and uses a different seed from the delivered package. The package inherits a fourteen-root/one-control description and two replicates despite its six-draw E14 design. The generic `collect.validate` also rejects the extra E14 key, requires task family metadata and permits at most four replicates; merely changing a number cannot make it the E14 adapter. Define the E14-specific accepted schema and connect the mock path to the same frozen release/config/task inputs that its future adapter will consume. Preserve public/private roles and original collectors. Hash the actual supplied spec, including alternate paths; refuse input drift before dispatch. Compare both recorded seed labels and actual numeric request seeds against a complete, declared historical inventory. Missing logs must be a reported unresolved dependency, not silently treated as no collision. Reproducible seeds and noncollision do not establish independent draws.

4. **Demonstrate guards and total accounting with fakes.** Model/template/server/request guards must reject injected mismatches, not merely appear in metadata. Exercise byte/context, per-request, phase and outer limits and all attempted/uncertain starts through a connected ledger. The future 228-start inventory remains 18 containment + 40 instrument + 30 grader rechecks + 140 candidate. Fake boundary events may exercise that ledger; actual starts remain zero. Reconcile the package's 600-second grading and 1,560-second budget fields with the contract's 300-second grading and 2,700-second outer proposal; state which limits govern each actual phase. Eliminate unsupported commands/flags and stale claims that delivered files do not exist. Proposed limits remain planning until an explicit later release.

5. **Make source reproduction portable and leakage checks meaningful.** Separate stable content identity from local cache-location metadata, or document and test canonical staging. Identical bytes at the two known cache locations must produce the same release payload. Test decoded public fields and rendered example content for deliberate scorer-only leakage; serialized-string matching alone misses escaping and representation changes. Preserve the MRL-23 partial package at its pinned revision; publish a versioned repair package with change notes and regenerated bindings. Do not alter the original ten-task contract, assertions or the historical E12/E13 packages.

6. **Correct scientific prose without releasing inference.** The target is the finite-history contrast between a fixed neutral instruction and a public-diagnostic-dependent instruction rule, not simply two constant strings. It does not validate a learned personalized policy or longitudinal policy value. Relating draws to conditional means requires the declared receiver-sampling law; independence of labeled seeds alone is insufficient. Delete the blanket statement that a bounded Hoeffding radius is necessarily vacuous at ten roots: under hypothetical independent equal-weight [-1,1] contrasts the usual two-sided 95% radius is `sqrt(2 log(40)/10)`, about 0.859. This arithmetic neither establishes the needed assumptions nor releases an interval. Keep this development stage descriptive, with no normal/Hoeffding interval, efficacy or futility claim. Historical mean prompt length gives an analogue-based cost scenario, not a lower bound for different tasks/answers. Describe the unchanged extractor correctly: raw text only when no triple-backtick marker occurs; ambiguous/malformed fenced output is rejected.

These changes repair the implementation of the declared E14 target; they do not change its hypothesis, source roster, endpoint, public information or fixed receiver. The lead's original finite-history and information-balance boundaries remain authoritative. Preserve all E11/E12/E13 negatives, the whole-source and twenty-record exposure disclosures, unresolved family independence, and separate future policy validation.

## MRL-24 bounded handoff

**PROCEED with one source/mock repair allocation for the existing worker: one sequential local CPU execution worker/thread, 30 elapsed minutes from its actual acknowledged start, $0.** No model/receiver calls or tokens; no reference, control, candidate, public-check or containment program execution; no downloads, installations, roster expansion, new outcome inspection or shared-resource reservation. Parsing, compilation without program execution, and tests using injected fakes are allowed. Any parallel reviewers must be read-only and must not launch concurrent local test/build workloads. Do not use a workflow with an automatic continuation budget.

Return a versioned source package, connected boundary tests and their complete pass/fail log, corrected proposal/command contract, exact source hashes, and actual command wall/CPU accounting. Prioritize criteria 1–3 if the cap prevents completion. Publish complete or partial/blocked work by the exact deadline, state unfinished criteria, and stop; no automatic renewal. Report a restart or missed scheduler tick truthfully rather than treating elapsed downtime as extra allowance.

Acknowledge **MRL-24 accepted/running/completed/blocked/superseded** with real UTC, full processed lead commit, exact start and deadline, package identifier, last actual scheduler tick and any current run/lease. A file-based response is sufficient. The lead will independently review the returned package before any instrument/receiver decision; this repair request does not grant those executions.

**58%, change 0 percentage points**, same fixed rubric and denominator. The reviewed partial package repairs preparation within existing credit; its new path was not previously credited as runtime-validated. No empirical claim or prior accepted result is invalidated by these new-path defects. Efficacy remains unestablished, independent policy validation is outstanding, and the full project is not submission-ready. Next milestone: a reproducible, source-bound mock path with correct private-score and failure accounting, followed by a separate bounded instrument-validation decision.
