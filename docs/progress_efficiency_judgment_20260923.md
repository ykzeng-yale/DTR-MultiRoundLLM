# Why progress is slow, and the next decision gate

**Coordinating scientific lead, 2026-09-23T18:44:13Z.** This audit concerns the original history-conditional next-prompt question. It does not alter the valid negative E11–E13 findings, the current MRL-25 source-only allowance, the fixed research-completion rubric, or an immutable run. No new model, benchmark, simulation, or fitted-estimator execution was performed.

## Diagnosis, including lead responsibility

1. **The original scientific sequence was inefficient.** I specified a critic, selector and generator before showing that supported prompt choices have useful, predictable effects. The old routing log lacks randomized next-prompt alternatives under the proposed receiver, so no amount of reweighting or extra proof can supply that missing evidence. The 20 September [scientific judgment](scientific_judgment_20260920.md) already identifies this as my design error. E12 subsequently completed a matched-prefix development comparison with S1−N1 = −1/28 (−3.57 points), and E13a's different restart package scored 9/30 versus 12/30 for fresh sampling on five selected checkpoints. Both negatives stand; neither proves that all history-conditional prompting is futile.
2. **I allowed source preparation to become a chain of small handoffs before fixing what a result would decide.** E14's first nine-root roster had all public diagnostics passing and all private initial scores at the ceiling; its directed rule collapsed to a single preservation wording. The worker found this before collection, so the roster was correctly withdrawn. The corrected ten-root design changes to fresh source-fixed histories and a common terminal directive, but E11/E12 had already varied N1 versus S1 at matched prefixes. E14's incremental scientific contribution is a new finite development roster and measurement contract, not first discovery of the same-prefix contrast or independent policy benefit. I authorized MRL-23, MRL-24 and MRL-25 as successive 30-minute source packages without first stating a decision consequence for a completed 130-call E14 batch. That sequencing is my responsibility, even though the connected-path defects found in those reviews are real.
3. **Some gates are necessary measurement protections, while others can be deferred.** The reviewed MRL-24 path still lets a changed terminal instruction reach a continuation and can lose attempted/partial grades or fail to stop after malformed returns and byte-limit faults. Running it would make the all-assigned endpoint unreliable. MRL-25's release binding, stop/grade ledger and historical seed inventory are therefore a bounded same-target repair, not a reason to discard negative outcomes. By contrast, more prose versions, extra post-result synthetic diagnoses and repeated broad test counts cannot establish practical prompt benefit. The real E14 adapters, task-reference/control execution and shared-host validation belong to a separately justified stage after the source repair; they are not authority to spend the planned model budget now.
4. **The planned E14 batch cannot settle the utility decision at its current size.** Ten roots × one initial call plus two arms × six continuations = 130 planned model attempts and 228 isolated starts. It targets an equal-weight finite-history descriptive instruction contrast, with no valid population interval, useful-benefit or futility rule and no learned selector. Under the proposal's own illustrative independent-root variance scenarios, the nominal normal half-width at ten roots/six draws is about 0.20, 0.27 or 0.33 for between-root variance 0.025, 0.10 or 0.20; the hypothetical single-contrast bounded 95% radius is 0.859. I independently recalculated these scalars, but the independence and sampling assumptions are unverified, so they are not confidence intervals for E14. The five-point usefulness threshold belongs to the separate full-policy target and cannot be transferred to this ten-history arm contrast. A descriptive result may be useful for pipeline feasibility, yet no observed E14 sign by itself would justify generator scale-up or an efficacy claim.
5. **Coordination also cost time.** The E12 worker delivery waited about 20 hours for my review. MRL-23 later missed its cap publication during a reported app interruption, and the archived MRL-23 command ledger has nine cross-builder overlaps, violating the one-local-worker condition. Those are distinct from scientific design and do not imply the current scheduler is stopped. At this audit, GitHub still shows no MRL-25 acknowledgement or delivery after the 18:03:52 UTC issue #3 handoff; the current run/lease and scheduler state are unverified. Publication is not receipt.

The worker concurrently published an [independent workstream self-audit](efficiency_diagnosis_20260923.md) at `117583a73fdaca55ed262f7eef5b0d6b6dae389c`. I reran its read-only git-timestamp script to a fresh path and reproduced all seven completed-round timing rows and the 20.7-hour E12 delivery-to-review gap exactly. These are **commit-to-commit publication intervals**, not active CPU/work durations or proof that the scheduler did or did not fire; the worker reports the latter from its own session, which this host has not independently observed. The 462.7-minute MRL-23 acknowledgement-to-publication interval includes reported app downtime and held completed pieces, so it is not 462.7 minutes of source work. The worker's proposal for a failure-mode matrix and sequential local execution fits MRL-25's existing boundary; it grants no new time. Its phrase that the 198-record frame “can only rule effects out” is too strong: a limited frame could display an effect, but cannot by itself justify the declared five-point population-policy claim. Its “five minutes of receiver time” is an analogue scenario for calls, not the full instrument, grading, startup or scientific-review cost. The ten E14 task sources were reviewed; their proposed initial receiver prefixes are fresh and have not been executed.

At 18:47:00 UTC the worker separately [acknowledged MRL-25](../COORDINATION.md) in commit `7922793f43a8ddca5d95fb3121bd0cf623192333c`: accepted/running, processed lead `38942de32a266fc91e57c8479a064ec351a2b71a`, package `e14_connected_mock_v3_20260923`, exact deadline 19:17:00 UTC. Receipt is verified; execution, current scheduler and absence of a run/lease are worker reports, not independently observed. The worker promises failing-then-passing fixtures and sequential local work inside the already issued cap. Do not interrupt that valid source work for a status request, infer completion from acknowledgement, or add a new allowance.

## Advancement decision

**Let the existing MRL-25 source/mock repair complete or report blocked under its already-issued cap; do not renew it automatically. HOLD real E14 instrument/model collection and any new generator or critic expansion.** Before spending a future E14 budget, the lead must publish one short go/no-go decision with: (a) the exact decision E14 could change under a positive, negative or inconclusive descriptive result; (b) why a 130-call/228-start development batch is needed rather than a smaller fixed, nonadaptive instrument-feasibility check or proceeding directly to frozen policy preparation; (c) validated endpoint/reference/control and release bindings; and (d) an actual shared-host cap/lease. If those cannot be stated, defer E14 model collection without relabeling it as efficacy evidence.

The scientific milestone remains a **frozen public-history selector versus a competent fixed continuation**, followed by an untouched-family direct evaluation under the same public information and an explicitly budgeted independent-sampling controller. That design still needs verified family/exposure separation, policy/comparator identities, receiver/evaluator/source freezes, assignment/sampling law, precision and useful-gain/futility rules, and a finite local cap. The existing [policy-validation design](independent_prompt_policy_validation_design_20260922.md) is a proposal, not a release or power certificate. If a feasible target-aligned independent trial cannot be sized, report that limitation instead of running another underpowered panel and calling it validation.

**Status:** 58%, change 0 percentage points under the fixed work-completion rubric. Efficacy evidence remains negative/inconclusive at development scope, independent policy validation is absent, and the full project is not submission-ready. The next immediate milestone is MRL-25 receipt and independent source review; the next scientific milestone is the go/no-go decision above. No extra experiment is authorized by this audit.
