# Scientific diagnostic plan, 2026-09-20

Status: exploratory diagnosis of already analysed data, recorded before the new sensitivity runs. No fresh-confirmation claim is possible. The coordinating scientific agent owns the design, interpretation, and choice of next experiment; delegation does not transfer that responsibility.

The next analyses test our own interpretation of the repaired results. They will not select a better-looking split, winner, endpoint, or budget for the original claim.

1. Decompose fixed-receiver repair versus initial-answer success into corrected-error gains, introduced-error losses and finite-sample IPW baseline imbalance, over all root tasks. Separately diagnose public-check false passes and false failures. Conditioning on intermediate outcomes gives descriptive mechanism strata, not separately randomized causal mediators.
2. Audit what the recorded success and visible validation actually measure, whether tests overlap, what zero checks mean, and whether assertion-level regrading is possible from existing data.
3. Stress-test the coordinating agent's positive fixed-four-candidate logistic result. Preserve the original split/result. Refit the same frozen logistic procedure on seeds 20260920–20260929; report all 10 splits for both models, their range and median, without pooling overlapping test roots into a new sample size or selecting a winning seed. On the original split only, compare full, public-check-only, code-structure-only and token-only features. These post-hoc diagnostic ablations are not a new primary analysis or model-selection trial.
4. Reconstruct the original scientific chain: identification → accurate conditional contrast → observable effect modification → better policy → acceptable total cost. A valid theorem at one link does not establish the later links. Assign each observed failure to a broken/untested link and identify the smallest prospective falsification study.

All analyses are CPU-only, $0 external spend, zero new model calls. No candidate-generated code will be executed. Existing raw data and results stay unchanged. Current full-project readiness remains below submission; the new work improves diagnosis, not confirmatory evidence.

## Added diagnostic before matched-estimator execution

The earlier E0 plug-in fit used all observations while DR used fold-specific fits, so their RMSE comparison does not isolate augmentation. Run 80 replicates each of base, uniform logging, latent-dependent logging, template-mixture variation and weak overlap. Within every replication, fit one identical compressed-state Q per task fold and compute both its plug-in and DR scores on that fold. Compare bias/RMSE against the same actual truth and paired squared-error differences with Monte Carlo uncertainty. Report all five cells. Known logging propensities remain known; this is not a fitted-propensity study or policy-ranking experiment. No plug-in Wald interval is justified merely by its root-score variance when nuisance-fitting uncertainty remains.
