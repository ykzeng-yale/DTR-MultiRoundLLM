# Matched numerical runner: source acceptance only

23 September 2026. Lead-owned issue #2 work for the unchanged [prospective numerical design](fitted_history_numerical_design_20260922.md). This package connects estimation and reporting; it does not release or execute the sampled comparison. The 600 planned datasets remain ungenerated.

## Connected scientific path

The [job adapter](../experiments/e0/regularization_job.py) binds each job to the committed plan and its declared data/fold seeds before seeing outcomes. Production uses the specified 230 roots, 40 episodes per root, three turns, five-action uniform target and three logging laws. Each dataset receives one root-fold assignment shared by all four nuisance variants. Matched plug-in and DR calculations share the fitted Q within each variant. Point estimates and DR standard errors use equally weighted root means. The 1.96-SE interval is the procedure under investigation, not a demonstrated coverage guarantee.

Saved provenance includes actual dataset and fold hashes, source/version identifiers, root means and fold-level support diagnostics. A prospective pairing identifier is distinct from the subsequently observed data/fold hashes. Folds or seeds do not by themselves establish independent empirical validation. The original simulator and estimator sources remain unchanged.

The [runner](../scripts/run_regularization_comparison.py) saves all 600 planned jobs before computation and durably records the eight estimator slots as attempted when their shared dataset job begins. It saves each completed variant immediately, including point or interval failures. Callback identities, source hashes and data/fold bindings are checked before accepting records. Duplicate terminal records and incomplete nominal job returns are rejected. A stopped job retains completed variants; the remaining started slots are interrupted attempts, while subsequent jobs remain unattempted. The reporter receives one final state per slot during normal completion.

## Resource and publication gates

Production requires a committed `e0-regularization-execution-freeze-v1` with `execution_released: true`, exact source hashes and source commit, Python/NumPy/SciPy versions and PCG64, and the unchanged caps: one worker, 600 outer seconds, 268,435,456 output bytes, zero model calls and $0. No such release is supplied by this source acceptance. The source commit must be an ancestor of current HEAD and its pinned bytes must still match. The separate resource receipt must match the host and freeze, be at most 300 seconds old and contain concrete availability/lease inspection evidence. It is an operator assertion requiring truthful inspection, not an automatic detector of competing jobs.

The parent starts its clock before scientific imports and supervises one child process group. Numerical thread settings are one; no parallel job pool is used. The deadline includes source checks, imports, deterministic truth, fitting and reporting, with cleanup time reserved. Writes count existing and temporary artifact bytes before writing and reserve 4 KiB for the supervisor receipt. Only a new output directory is accepted; there is no retry, resume, extra seed or automatic budget extension. These are cooperative bounds for the pinned numerical worker, not containment for arbitrary hostile programs. The supervisor independently detects output drift, including a fast child that exits before the next poll.

An interruption can leave the final summary absent. Preserve the plan, completed journal lines and supervisor receipt; reconcile them in a later source-only analysis without rerunning seeds. Never treat absent summary output as zero attempted work. If a write itself is interrupted, a partial final JSONL line requires an explicitly documented recovery decision; earlier valid lines remain immutable. Incomplete-run summaries describe the observed prefix and do not establish the planned unconditional Monte Carlo precision.

## Validation boundary and next milestone

Independent scientific review accepts the production parameter, shared-fit/fold, equal-root and failure-accounting path. Operational fixtures exercise deadlines, termination, byte limits, source bindings and resource-receipt rejection. Connected fixtures replace the scientific job with supplied records; adapter fixtures use handcrafted data and mocked sampling, including real tabular fits and deterministic fold permutations. These are source/mock checks, not sampled simulation results or evidence of lower error or correct coverage. Exact test commands, timing and source hashes are in the [validation record](../results/e0_regularization_runner_validation_20260923.json).

Next: commit the final source/environment execution freeze and inspect current resource availability before any bounded sampled run. Any partial run then needs independent journal reconciliation and the prespecified incomplete-study qualification. MRL-23 experimental delivery remains a separate dependency; this work does not duplicate or renew the experimental worker's allowance. Overall research completion remains **58%, change 0 percentage points** under the fixed rubric. Prompt efficacy is unestablished and the full project is not submission-ready.
