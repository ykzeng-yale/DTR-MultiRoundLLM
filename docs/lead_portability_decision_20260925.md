# LEAD-PORT-01: clean-checkout reproducibility repair

25 September 2026, 22:13 UTC. **Proceed with one bounded, source-only repair by the existing experiments worker; HOLD every model/instrument collection stage.** This responds to worker `e250b8f`, which accepted the lead's E0 supervisor repair and reported a clean worktree of `5d80ce2` with 6 failed, 7 errors, 1,625 passed and 28 skipped, while its populated host passed 1,666 tests three times. The lead's earlier host had six failures in the same builder/inventory/E2 areas. These are software reproducibility observations, not E14 validation or prompt outcomes. No new worker or experimental collection job is created.

The permitted repair is limited to three concrete defects. First, tests that require the ignored MBPP source cache may skip with an explicit missing-source reason on a clean checkout; the actual package builders and `verify` entrypoints must still **fail closed** without the exact source. Document one source-acquisition/check command with URL, license and SHA256, and verify the hash before any rebuild. Do not claim that a skip reproduces the committed E14 or v3 package. Second, make the seed-inventory test's claim match committed files, while retaining the original host-local 19-log inventory and the lead's separate 13-log Git-blob inventory as distinct artifacts. Third, make E2's test/builder portable without silently rewriting an executed, hash-bound fixture: preserve the original frozen bytes and records, and use a versioned or runtime-rebound projection with explicit before/after hashes if needed. No private assertion, endpoint, task, receiver law or E14 release artifact may be changed under this allowance.

**Acceptance evidence:** show named tests failing/erroring in a clean checkout before repair, then passing or explicitly skipping there afterward; show the populated checkout still passes the affected tests; verify that every committed source/mock package and frozen executed evidence artifact retains its byte hash. Report exact skipped tests and why, and keep any remaining external-source requirement visible. Run sequentially with at most one local CPU test worker, 15 minutes wall, 256 MiB of newly retained local test outputs, zero model/receiver calls, zero paid spend and no shared receiver lease. If a source download is required, stop after documenting the pinned acquisition command and request a separate review; this allowance authorizes no download or benchmark execution. Return accepted/running/completed/blocked/superseded with real UTC, processed lead SHA, commits, before/after clean-worktree evidence and unresolved failures.

This is a reproducibility gate for future review, not a release of E14's 130 calls/228 starts, a new independent-family trial, or a change to the original history-conditional same-prefix target. The separate lead-owned selector, fixed comparator, family, endpoint, inference and full local collection cap remain open. **58%, change 0 points; prompt efficacy unestablished and full project not submission-ready.**
