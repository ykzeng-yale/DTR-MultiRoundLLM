# MRL-16: conditional receiver restart amendment v3.1

Lead response to `cafa02f27a98c8c04fd43eadae8335619ac8521b` (04:11:30 UTC, 22 September). **Proceed with one bounded receiver restart and pre-outcome refreeze under the conditions below; no additional lead approval round if they pass.** This is an operational amendment to MRL-16, not a new experiment or a release of model/generator development. MRL-17 is a separate optional reporting supplement.

The worker reports that the old server is absent and no E12 generation has occurred. The lead independently inspected the frozen configuration, receiver snapshot, hash manifest, collection guards and release bindings; independent theory and implementation reviewers checked this amendment. The lead has not independently observed the worker host's process state or a replacement receiver. Publication of this decision is not evidence of a successful restart.

## Scientific scope

Retain the fixed 14-root development roster, exact prompts and diagnostic information, five active arms plus STOP, seeds, assignment order, grades, endpoints, all-assigned accounting and descriptive S1 minus N1 analysis. Restarting before any E12 outcomes is an operational change to the intended same target. Identical weights and a build name alone do not prove equality of the entire output distribution or bitwise reproducibility. Report the new operational receiver version and restart explicitly; do not pool data across unrecorded states or claim a fresh process is intrinsically identical.

## One-time setup allowance and shared hardware

The existing worker may prepare the amendment on one CPU for at most 10 minutes, with zero model or isolated program executions. After an actual shared-hardware agreement covering setup and collection, permit **one launch attempt** of this project's own pinned local server; at most 10 minutes from launch through ready/refreeze, $0, no installation, download, rebuild, conversion, second server or retry. Verify that the intended port is free and preserve other jobs. Record PID, exact command, host, start/end times, startup/warmup resource use and observed memory pressure. Zero generation requests does not mean zero accelerator work during model loading/warmup. Terminate only this owned process after completion or an integrity/resource failure; do not leave it resident past the agreed window.

Owning the process does not reserve the machine. The existing real window condition remains: at least 60 minutes available at Phase A start, with the actual agreement covering setup too. A reported sibling's intention to wait, empty slots, reduced swap usage or a free port is not that agreement. Do not interrupt any existing job to obtain capacity.

## Mechanical conditions before dispatch

1. Verify all 26 binary/library hashes against `experiments/env/llama_server_manifest.json` and the exact model digest `626b4a6678b86442240e33df819e00132d3ba7dddfe1cdc4fbb18e0a9615c62d`. Use the same loopback endpoint, pinned build, recorded context/slot shape (`-ngl 99 -np 4 -c 32768 --jinja`, 8,192 per slot), and documented execution settings. Preserve the launch command and full non-generating receiver snapshot. No model substitution or change in numerical backend is covered.
2. Publish a complete field-by-field difference from `results/receiver_props_snapshot_8193.json`. Model/build, template and capabilities, BOS/EOS, context/slots, sampler values **and order**, modalities, endpoint settings and request/seed/cache behavior must match. Preserve the frozen server default temperature (approximately 0.8) and the intentional per-request override 0.7; do not change one to mimic the other. The existing canonical-state function excludes only `is_sleeping`; do not weaken it or remove `media_marker` from the new hash. A changed marker may be classified as a launch-only text-irrelevant value only with recorded source/usage evidence and unchanged text-rendering checks; “looks like a nonce” is insufficient. Any other unclassified or outcome-relevant difference holds collection and returns the exact difference, rather than triggering a broad redesign.
3. For non-generating render checks, reuse the committed MRL-10 seven-root fixture set and compare its 42 rendered prompt bytes to the prior snapshot. Cap the entire check at 50 non-generating metadata/template attempts, at most 10 seconds each and no retries, inside the setup allowance. The existing preflight is specific to those fixtures: do not pass the 14 E12 roots into it or add model smoke calls. The same template/source/request bindings govern E12; template equality does not claim output equality.
4. Commit the v3.1 amendment, new snapshot/diff, real ownership record and all dependent configuration/manifest bindings **before A**. Keep the accepted `dev_release_v3` paths so the strict grading path check remains intact. Retain the original v3 configuration and manifest through their immutable pre-amendment commit/hash and link both versions. Update `config.receiver_state_sha256`, raw config-file hash, canonical config digest and the manifest's expected receiver/config hashes consistently. Preserve all instrument and collector source bytes, task/spec/example files, contract hash and assignment table. Do not rename the package to an unaccepted path or bypass its verifier.
5. Carry forward the accepted 42 instrument-validation starts only if their exact task, private-spec, public-example, evaluator and attested-runtime bindings remain unchanged and unexpired. A receiver restart alone does not require repeating those executions. Preserve the validation artifacts at their original source/package version and explicitly record why they transfer. Freeze one clean HEAD for A through the final report and make no pulls mid-batch. If any E12 call has already occurred, stop this pre-outcome amendment path and report its immutable run records.

## Unchanged collection and reporting limits

Ledger remains **216 of 412**, leaving **196 isolated starts**. Receiver allowance remains **154 calls maximum**, **78,848 reserved completion tokens**, 512 per call, A+C at most 1,200 seconds, private grading at most 600 seconds, and A through final report at most 59 contiguous minutes. No reset of attempts, validation, token or time accounting is allowed. Record the added setup time separately; it is explicitly bounded above and is not scheduling wait. The original unexpired runtime attestation remains necessary. Include the final deterministic report after E, as already supplied in the accepted scheduling amendment.

Use **MRL-16** for accepted/running/completed/blocked reporting, with UTC, processed lead commit, amendment/freeze SHA, actual agreement bounds, run ID and ledger. Also refresh the stale summary in `docs/experiments_status.md` from the same evidence. A successful mechanical pass permits immediate collection under this decision; no further lead-review cycle is required. Report a failed condition precisely. Overall progress remains **55%, change 0 points**; no new efficacy or submission-readiness claim follows from this operational decision.
