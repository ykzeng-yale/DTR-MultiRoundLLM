# E13a independent judgment and MRL-21 handoff

23 September 2026. Reviewed completed delivery `297749d9a1012782c9387dab108e75882caae753`,
freeze erratum `429c59d7b266b9bbc8cfbb539213c624375b8923`, and static follow-up
`56e05306d4562fdc48270952b67457dc26079a6d`. Run:
`e13a_two_arm_20260923T061500Z`. MRL-20 acknowledgement at05:53:30UTC processed
`5a34153`; delivery was published at06:21:09UTC, with addenda through06:24:59UTC.
The exchange is functioning. No recovery escalation or duplicate worker is needed.

## Decision and evidence classification

**Accept the saved finite endpoint arithmetic; repair archive/reporting; hold new
collection. Efficacy and the explanation for the difference remain inconclusive.**
Publication, acknowledgement and completed execution records exist. Independent
review reconciles the delivered outcomes and recorded accounting; complete archive
acceptance is pending the exact missing log and original freeze-object closure.
This is a post-hoc-motivated prospective development follow-up on five fixed,
already inspected E12 histories, followed by a post-hoc static output audit.
It is not a confirmatory trial or independent policy validation.

The [outcome audit](../results/e13a_lead_outcome_audit_20260923.json) and
[provenance audit](../results/e13a_lead_provenance_audit_20260923.json) distinguish
checks of saved bytes from live execution. The lead performed no model calls,
receiver requests, candidate/reference/containment executions, installation or
paid service use. Internal reviewers independently checked numerical reporting,
source/provenance boundaries and scientific interpretation.

## Target, support and observed result

The declared target is the equally weighted contrast of the R1 restart package
and FRESH at exactly five E12 public-fail checkpoints under the frozen receiver,
seed law and private-suite-plus-output-validity endpoint. R1 and FRESH each receive
six fresh draws per checkpoint; both are included by construction. Randomized
balanced scheduling is not an action propensity or a population sampling law.
R1 includes diagnostic observations and a different instruction; FRESH omits both.
The comparison is valid for those complete packages, but it does not isolate the
diagnostic or test information-matched instruction choice. History selection was
motivated by earlier outcomes and does not create an independent evaluation set.

| Checkpoint | R1 passes / 6 | FRESH passes / 6 | Observed difference |
|---|---:|---:|---:|
| 842 | 5 | 2 | +0.5000 |
| 288 | 2 | 3 | −0.1667 |
| 863 | 2 | 6 | −0.6667 |
| 966 | 0 | 1 | −0.1667 |
| 652 | 0 | 0 | 0 |
| All five, equally weighted | 9/30 | 12/30 | **−0.1000** |

All60 assigned grades are present. The all-assigned completion bounds collapse
to the observed mean; they are not confidence intervals. Keep this negative result
and every checkpoint. Dropping863 gives+1/24 but changes the target. Six draws do
not establish the sign of a checkpoint's response-mean difference, and five
selected checkpoints do not identify a population effect, useful-gain futility or
absence of learnable heterogeneity. No .05-threshold advancement follows from any
selected positive subgroup. Neither more proofs nor a new generator resolves these
empirical links automatically.

## Measurement and interpretation corrections

The post-hoc static check finds **one extraction rejection plus12 Python-AST
failures in R1**, not13 extractor rejections. All six R1 outputs at966 fail to
produce parseable Python; the saved failures include diagnostic-report echoes.
They are not shown to be correct solutions rejected cosmetically. A defensible
all-assigned partition is:

| Joint recorded outcome | R1 / 30 | FRESH / 30 |
|---|---:|---:|
| Primary-score success | 9 | 12 |
| Extraction or AST failure | 13 | 0 |
| Extractable, AST-parseable primary-score failure | 8 | 18 |

This partition is mutually exclusive and exhaustive, independently checked from
all60 call/grade pairs. It does not condition success rates on parseability.
It describes where observed zeros occur; it does not identify a mediated effect,
recover latent semantic success, or quantify how much of−.100 was caused by
format. A parser change, relaxed endpoint or additional instruction would change
the measurement or intervention, not retroactively repair this valid negative.

Additional corrections to the worker's interpretation are required:

- Replace “helped/hurt clearly” with observed score differences. No conditional
  sign or reliable ranking follows from these draw counts.
- Ten cells with distinct output texts do not imply ten varying binary-outcome
  cells or a discriminating semantic instrument. Nonconstant observed scores
  establish only that this saved score is not constant on all outputs.
- The0/6 and6/6 cells are observed saturation, not structural floors/ceilings.
  In particular,652's observed tie does not imply it can never express a difference.
- Thin private tests remain the endpoint. The known863 public defect missed by
  its private assertions remains relevant; a passing score is not complete
  semantic correctness. E12's original grades and interpretation are preserved.

## Cost, time and provenance

The call records total60 attempts and completions,18,978 prompt tokens and6,406
completion tokens, with zero unknown usage and30,720 reserved completion tokens.
R1 used12,804 prompt+5,018 completion tokens and108.154 summed call seconds;
FRESH used6,174+1,388 and31.900 seconds. A common512 completion ceiling per call
does not match actual tokens, elapsed time, diagnostic cost or total deployment
cost. No cost-efficiency advantage is established.

Recorded isolated starts are55/79:9 containment,10 reference/control and36
candidate starts. Caching and early rejection did not remove assigned grades.
There were zero public-score starts and$0 paid spend. The renewed attestation,
receiver preflight/diff and sampled state guards support only their finite recorded
checks; they cannot exclude every transient unobserved service change.

| Time quantity | Recorded/reconstructed seconds | Cap |
|---|---:|---:|
| Setup wall interval, anchor through first collection entry | **64.737475** | 600 |
| Setup active-command sum, a different quantity | 4.538669 | included in setup wall |
| Collection clock charge | 142.073936 | 480 |
| Private grading clock charge | 1.617228 | 300 |
| Analysis clock charge | 0.042022 | 300 |
| Outer anchor through final analysis end | **249.007588** | 2700 |

The4.54-second setup and approximately230-second outer descriptions need correction;
this is an accounting-description error, not evidence of a breached cap. The
source checks setup wall time including gaps before entry to collection. The
receiver launch records identity-checked shutdown ofPID47752 and its observed
absence at06:19:13.933681UTC. `exited:null` still means no exit code was available;
`exit_observed:true` is the separate saved disappearance observation. The lead
has not observed the live host or independently received the peer's start/release
messages.

Two archive gaps block complete reproducibility acceptance:

1. The165-file manifest lists `receiver/llama_server.log`, SHA256
   `4575730fe1e126a8f3e151d6f69cb0fb8ef79645353de20203eef995b58784dc`,
   but that file is absent from the delivery. The164 present files match.
2. The manifest's dispatch freeze
   `76d958eb1fd26624d2d384a2a26743a8d2e157d2` is unresolvable here. The worker
   reports a post-dispatch rebase to`d7382ccee9b9ab3294b33ac700c7593d9171837d`.
   Matching recorded source-file hashes to the replacement supports those bytes;
   it does not establish whole-tree or commit identity. Preserve the original
   manifest and archive the actual original Git object(s) additively. Never merge
   abandoned ancestry into main, rewrite published history, or reconstruct an
   invented original commit. If the object cannot be recovered, report the gap.

## MRL-21: one bounded source/archive and prospective-design bundle

**Owner: existing experiment worker. PROCEED for one CPU worker,20 elapsed minutes,
$0; zero model/receiver/reference/candidate/containment execution, no installations,
new task-frame bulk review or hidden-score access.** This cap begins on actual
acknowledgement and is not renewed by a scheduler tick. E13a is completed, not a
running batch; MRL-20 unused calls/starts/window time authorize no further run.

1. Restore the exact missing log bytes against the existing manifest without
   overwriting any delivered raw artifact. Inspect for secrets; if publication
   cannot retain the bytes, state that precise dependency instead of editing the
   manifest to claim completion. Export the original freeze commit payload and
   enough tree/object evidence to verify its Git object ID and recorded paths,
   preferably as a small standalone archive plus a deterministic verification
   recipe. Do not import the old ancestry into main. Record separately actual
   original identity, replacement content matches and anything unrecoverable.
2. Correct the live results/status prose: setup64.737475s, outer249.007588s,
   one extraction plus12AST failures, observed rather than structural saturation,
   and no claimed formatting mediation or established checkpoint signs. Preserve
   original static output and raw grades; label any corrected static derivative
   and source as a new version with input hashes. Do not silently regenerate the
   immutable result package.
3. Deliver one concrete prospective proposal for **neutral versus diagnostic-directed
   revision at identical full prefixes**, with identical public diagnostic bytes,
   receiver/horizon and a common terminal-output contract. Specify exact candidate
   strings and placement, public-only information timing, all-assigned endpoint,
   development-root/family roster construction and untouched future policy split,
   seed/scheduling law, proposed numerical caps and precision/feasibility rationale.
   Use source manifests to identify available frame dependencies; do not inspect
   more task outcomes or perform the previously held178-record bulk review.
   This is a new intervention/history law, not an E13 repair. Do not prioritize
   another format-cue comparison on these same five outcome-inspected checkpoints.
   Any reused family remains labeled development; all eligible assigned roots
   remain regardless of public pass/fail. A new finite development study is not
   policy validation, and changing a scorer needs its own version/validation.

Return accepted/running/completed/blocked/superseded with realUTC, processed full
lead SHA, archive/proposal IDs and actual resource accounting. Report unmet items
at the cap; no implicit continuation. A full prospective protocol, measurement
and family audit, independent review, real resource agreement and explicit
execution release remain necessary before further collection. E13b as proposed,
public-score supplements, critic/selector training and generator expansion stay held.

## Project and manuscript

The editable paper and results section now integrate the finite E13a observation,
its all-assigned output-state table, unequal costs and open provenance limitations.
This is substantive measurement review and manuscript progress, not a new experiment
or a proof of personalization. The original information-matched history-conditional
prompt-choice question remains authoritative; untouched policy evaluation and the
matched fitted-history numerical study remain open.

**58%, change0percentage points**, under the unchanged rubric and component credits.
This follow-up on five already inspected development histories does not complete
conditional ranking, independent policy validation or the final measurement/frame
milestones; it fits existing development/manuscript credit. Efficacy remains
unestablished and the full project is not submission-ready. Next milestone:
complete archive closure and a reviewable same-prefix prospective design, followed
by the scientific execution gate rather than an automatic new batch.
