# LEAD-POLICY-11: one-redraw public-check controller for the practical comparison

25 September 2026. **Prospective comparator-algorithm decision; HOLD all new collection.** This selects an explicit `b2` *design* for the separately proposed practical comparison. It does not freeze a trial, certify comparator competence, alter E11/E12/E13a outcomes, or replace the primary history-conditional same-prefix next-prompt comparison `d` versus fixed `b1`. A later change to this algorithm after evaluation labels would be a new comparison, not a correction of this one.

**LEAD-POLICY-12 same-target contract repair (25 September, 16:36 UTC).** The original wording incorrectly imported a three-public-case count from an earlier proposal. The saved E12 diagnostic has one public case on each of 14 roots (SHA256 `e95d50bae632f73a96094673a03aff9401993c5c3d986086f63c5fe3cf8bd94a`); the E14 source has one on each of 10 roots (SHA256 `900b97fb2f9ee7faa36920fdd44ea92841a86fbf322d01591115a07f6ce4f727`). The diagnostic accepts a nonempty case list, so the operative STOP rule is **every public case in the frozen record passes**. The other MBPP assertions are private and must never enter this controller. This repairs the intended all-public-pass algorithm without changing its target or using evaluation outcomes. The future source freeze must pin each root's public-case list/count and a distinct B2 redraw seed label; the existing seed table has no such label. No collection is released.

## Input, decision and terminal output

Use the same landmark public record `X` offered to `d` and `b1`: the original public task and examples, one initial receiver answer, and the canonical executed public diagnostic computed on that answer. Do not restrict to public-fail checkpoints; a future evaluation frame must include both pass and nonpass histories under its prospectively frozen checkpoint law. This avoids making the controller's apparently favorable subset depend on a post-sampling gate. The public case statuses and their canonical record follow the versioned diagnostic law in [`diagnostic.py`](../experiments/landmark/diagnostic.py), SHA256 `2a98bccf20cf3fa068790b488be8df4347387ae5ffb1bd7b1bd8b9d49d3a72b2`.

Define the **B2-1R whole controller** as follows, with no private grade, reference answer or other branch output at any decision:

1. If **every case in the nonempty frozen initial public-case list** has status `pass`, return the initial answer unchanged (`STOP`). A private-suite pass is neither required nor consulted.
2. Otherwise, make **exactly one** independent receiver draw using only the original bare public-task messages generated by `collect.initial_messages(task)` in [`collect.py`](../experiments/landmark/collect.py), SHA256 `49c94c45f856115925cf20560f0c43ba14348a859a041bc14ad9fd1431e7a83f`. Do not include the previous answer, diagnostic JSON, revision cue, hidden assertion or private score. Use the same frozen receiver/decoder law but a separately preassigned independent seed. Return this draw as the final answer whether it appears good or bad; no second draw, best-of-two private selection or outcome-dependent retry.

`wrong_value`, `format_error`, `interface_error`, `program_exception`, `timeout`, `unavailable` and `output_limit` are all **nonpass** for step 2. The displayed diagnostic is still the common pre-decision information and its acquisition must be charged. An absent initial artifact or failed diagnostic construction is not silently excluded or coded as a public pass; the full trial must freeze an all-assigned failure/missingness law before release. The final answer uses the same frozen extractor and private endpoint as the other policies, including any format failure. No post-redraw public checker is needed for this one-redraw acceptance rule; if one is run for audit, it must be invisible to the controller and separately charged as research expenditure.

## Comparability and cost

At the landmark, `d` and `b1` choose one same-prefix continuation from their supported recipe set; B2-1R may STOP or make one **bare-task** redraw. The `d`–`b1` contrast remains the prompt-choice mechanism test. The `d`–B2-1R contrast is a practical package comparison that also changes context and stopping; it cannot isolate prompt wording. E13a's FRESH arm is a different fixed resampling package on five inspected checkpoints and provides no estimate of this controller's value.

For a valid checkpoint, B2-1R uses zero or one **additional** receiver calls, so from task start its maximum is two including the shared initial call. `d` and `b1` each require the initial call plus one continuation. All three require the same initial public diagnostic under this chosen **unconditional** checkpoint law. The shared study acquisition/check is recorded once in research expenditure. Incremental deployment cost after `X` is zero or one B2-1R redraw versus one continuation for `d`/`b1`; end-to-end deployment cost includes the initial generation and diagnostic for all three. Report actual input/completion tokens, checker/executor starts, time, energy and dollars separately: a common two-call ceiling does not imply equal expense or efficiency. Do not force B2-1R to spend its unused call after an all-pass initial diagnostic.

This settles **only the controller's information, restart point, stop/accept rule and receiver-call maximum**. The exact task/family and public-case source hashes, receiver and evaluator versions, seed table (including a distinct preassigned B2 redraw label) and assignment probabilities, validated endpoint and missingness, independent-family sampling law, `d`/`b1` identity and training rule, useful-gain and simultaneous precision/futility rule, complete numerical *local* cap (including all checker and grading starts) and resource lease remain open. No E14/source renewal, model call or worker execution is assigned by this document. A future release must verify the bare-task message bytes and all policy paths against the committed freeze before collection.

Evidence class: prospective design based on the pinned renderer/source and the already reviewed E12/E13a limitations; **zero new model calls/tokens, benchmark executions, simulated draws, fits or paid spend**. **58%, change 0 percentage points**; prompt efficacy unestablished and full project not submission-ready.
