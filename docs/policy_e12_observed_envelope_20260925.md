# LEAD-POLICY-10: observed E12 decision envelope for the N1/S1 slate

25 September 2026. **Decision: retain NO-GO for using the existing two-recipe N1/S1 slate as the next independent policy-efficacy stage.** This is a post-result, reused-development-data calculation about the *observed grades*, not a population effect bound, a fitted policy, a futility test, or a new collection release. The original history-conditional same-prefix next-prompt question remains authoritative; the E11/E12/E13a negatives remain intact.

I recomputed the two-replicate mean for N1 and S1 on each of the fourteen E12 roots directly from [`analysis_input.json`](../results/e12_dev_v3_20260922T030255Z/analysis_input.json), SHA256 `4a64c49794c3951b28c0e232f8509700278bc057af598ec65599d15e4e516527`. The saved [`public-status audit`](../results/e12_public_status_signal_20260923.json) independently agrees on all root grades and the observed status groups. Fixed N1 totals 10.0 root-mean successes (20/28 continuation grades); S1 totals 9.5 (19/28). A hindsight rule allowed to choose the **higher observed two-replicate mean separately at each root** totals only 10.5/14 = 0.75, versus N1's 10/14 = 0.7143. Its observed gain is 0.5/14 = **1/28, or 3.57 percentage points**, below the working five-point useful-gain threshold on this descriptive root average. The only positive switch is root 842 (N1 0, S1 0.5); avoiding S1 on root 863 prevents a one-point observed loss. Twelve roots tie. Nine public-pass roots are 18/18 under each arm, so this thin saved private suite cannot reveal an N1/S1 difference there.

This is an **in-sample envelope for these fourteen estimated arm means and exactly these two supported whole-recipe indices**. It is not an upper bound on their true conditional mean effects, any new action class, a different endpoint, or the target independent-family population. It explicitly grants the hindsight rule private grades that a deployable selector cannot see. Two draws per arm leave large noise in the per-root means; all fourteen roots carry one unresolved-family placeholder. The fact that even this outcome-informed observed envelope falls short of five points strengthens the *design-priority* case against spending a new batch on the unchanged narrow slate, but it cannot justify statistical futility or a claim that useful prompt choice is impossible.

**Next discriminating action belongs to the lead:** define a mechanism-backed, exact public-only same-prefix recipe set and decision-time feature/selection law as a **new development design**, then establish a genuinely untouched family frame and validated endpoint. A fixed recipe comparator must be chosen only from development data; independent sampling requires its own frozen algorithm and fair resource ledger. Receiver/evaluator versions and hashes, assignment/seed probabilities, useful-gain and precision/futility rules, complete numerical cap and resource lease must precede any model collection. This note assigns no worker source job, grants no E14/model allowance, and does not replace the original target with fixed-bank selection or cost savings.

Read-only arithmetic on saved grades: **0 new model calls or tokens, benchmark-program executions, synthetic draws, fits, or paid dollars**. No new empirical efficacy or independent validation. **58%, change 0 percentage points** under the fixed rubric; full project not submission-ready.
