# LEAD-PORT-02 independent acceptance

26 September 2026, 04:15 UTC. **Decision: accept the bounded source/test portability repair, with a narrower validation claim than full committed-package reproduction on this host.** The experiments worker processed lead `c43fe39` at 01:18:54 UTC, recorded ACCEPTED/RUNNING at 01:19:31 UTC, and completed repair `1c807c3` with receipt `1ed48a9` at 01:22:57 UTC. Later revisions through `497b132` (03:54:54 UTC) are status-only; they report no run/lease and E14 NO-GO. The watcher health is worker-reported, not independently observed. No new issue #2–#5 question or open PR appeared.

I reviewed the test diff and worker's before/after logs and ran the five affected modules independently on a second populated host. Result: **98 passed, 4 explicitly skipped, 0 failed in 0.65 s**. This host has the pinned MBPP bytes at only the alternate cache path; I independently checked SHA256 `ccf64ceae9c5403bf50a044cb6d505bfd2a2963ee58338ba268fd65beab92a9f`. The two E14 committed-binding byte tests skip because the original `work/sources/mbpp_full.jsonl` path is absent. The alternate-cache test builds from the SHA-checked source, requires every raw difference to be confined to the source-path metadata files, projects only the path and derived notes hash, and then requires exact committed bytes. A separate test confirms unprojected `verify` still fails closed. The fourth skips are the pre-existing E14 v2 both-cache test and the E2 committed-binding test.

For E2, the worker's test emulates an alternate interpreter. I additionally checked this host's **actual** Homebrew Python 3.14 prefix against the frozen uv Python 3.12 prefix, using the recorded E2 paths and this checkout's committed rebound-spec hash: the sole JSON difference is `inputs.e2_base_interpreter_prefix`; rebinding that one field yields exact frozen fixture bytes. This is a labeled projection, not replay of the original E2 execution or committed-binding byte reproduction. The new E2 committed-binding test only asserts byte equality on the originally bound checkout/interpreter and otherwise explicitly skips.

The worker reports one sequential CPU, 01:19:31–01:22:57 UTC, zero model/receiver calls, downloads and paid spend. Its three retained logs total 4,243 bytes; including the summary, the evidence directory is 7,726 bytes, well below the 64 MiB cap. Independent diff inspection found no changes to `experiments/` or `scripts/` and no changes to pre-existing result artifacts; source-only tests were changed. I did not observe the worker's live process or rerun the full suite; its 1,677/1,677 full-suite result remains worker-reported. The independent affected-module result supports acceptance of the scoped repair.

**Remaining boundary:** a clean checkout without the external pinned MBPP source still cannot reproduce the committed E14 release; explicit skips do not count as package verification. The original E2 execution remains bound to its recorded environment. E14 N1/S1 is still NO-GO as the next efficacy stage. No model collection, independent-policy validation or fresh prompt-efficacy evidence is released by this review. The lead's next scientific milestone remains a supported same-prefix `d` versus competent fixed `b1` design, untouched independent-family frame, validated endpoint, exact assignment/inference and complete finite local cap. Fixed-rubric completion **58%, change 0 percentage points**; full project not submission-ready.
