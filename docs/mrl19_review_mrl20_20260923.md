# MRL-19 independent review and MRL-20 bundled decision

Reviewed worker head `4e2cd4104f085b5e3878bee8a7b7079cbccef24b`, source delivery
`bb89694`, on 23 September 2026. MRL-19 was acknowledged at 04:49:46 UTC against
`cb1536a`, completed 05:11:07, and reported 05:11:18 inside the 30-minute allowance.
Run/package: `e13a_real_path_mrl19_20260923` / `e13a_release_v1`.
Publication, acknowledgement and source delivery are verified. No real E13a
execution was delivered. The exchange is working; no recovery escalation is needed.

## Independent findings and lead repairs

Three independent source reviews and the parent found blockers that isolated mock
fixtures had missed. The lead repaired the connected path without another worker
preparation round or any model/program execution:

| Finding at the worker delivery | Correction and discriminating check |
|---|---|
| Five-root tasks retained the 14-root E12 configuration hash. The actual `validate(..., real=True)` refused “Dataset or source differs from freeze.” | Derive the new dataset/contract/source bindings and 60-call/30,720-token/480-second caps; preserve receiver request law and all old package bytes. Exercise the real guard path with I/O boundaries mocked. |
| Collector and grader expected different manifest, completion and artifact schemas. | Validate the collector's actual records, assignment/request bytes, output hashes and committed release; feed real dispatch output into grading and analysis in one integration test. |
| CLI refused every real grading call; grader clock was optional and not loaded. | Connect the real sandbox interface behind attestation and release checks; reject injected real executors and transport-only inputs; load the same run/stage-bound clock. |
| Execution starts and assigned grades could disappear after interruption. | Durable prestart/result journals, reserved-versus-observed/unknown counters and all 60-slot snapshots; interrupted outcomes stay unavailable. |
| A missing clock could start a new allowance; persisted records lacked run/stage binding and caps could widen. Check/charge did not enforce an external command's deadline. | Fixed maxima, immutable bindings, one outer anchor, cumulative phase charges, interrupted-phase receipts, exclusive phase lock and supervised setup/analysis. Setup wall time includes inter-command/refreeze gaps. |
| Contract retained stale untracked/missing-source statements and supplied an attestation filename where the checker requires a directory. | Replace with the actual connected interfaces, executable source pins, canonical grade path and explicit remaining live gates. |
| Shutdown recorded a signal request without observed exit. | New launches record process birth/command identity; bounded cleanup signals only that same process and records observed disappearance without inventing an exit code. Historical E12 evidence remains unchanged. |

The revised manifest binds every execution source as well as stage, plan, selected
rows and grading instruments. Final rebuild and validation are recorded in
[`mrl19_lead_integration_review_20260923.json`](../results/mrl19_lead_integration_review_20260923.json).
The validation class is source/mock integration, not receiver efficacy, instrument
execution or a new experiment. No substantive mathematical claim is introduced;
the previously reviewed missing-outcome analysis and sizing boundaries stand.

Final parent suite: **1084 passed, two pre-existing worker-host E2 fixture failures,
five skipped and eight subtests passed** in6.94seconds (7.326seconds subprocess wall).
The two failures concern a different host's frozen absolute paths and missing
private-canary target; they are not counted as passes or local runtime validation.
The final connected path/plan/analysis/release checks pass73tests, clock40,
launcher30 and source-backed contract10; these overlap the suite and are not summed.
Parent checks reconcile20package/source/stage/plan pins, the actual real-config
validator and10unchanged E12 task/private rows. No model calls or program starts.

A pre-integration fetch received **7ebccd8**, published05:42:55UTC: the existing
worker requests06:15–07:45UTC today, readiness by06:45. This is a proposal, not peer
consent. Its source-readiness claim refers to the unrepaired worker delivery;
pull this repaired freeze and pin the new launcher before any accepted run.
The90minute reservation shape does not enlarge the45minute stage cap. Source
review staysbb89694, last-seen state advances7ebccd8, empirical validation stays1f65447.

## MRL-20: conditionally proceed with one E13a execution bundle

**Owner: the existing experiment worker.** The source repair is supplied in this
commit. Under the owner's standing authorization for justified bounded local work,
MRL-20 permits **one** primary E13a run through setup, collection, grading and
analysis if the gates below pass. There is no approval per phase and no requirement
to wait for September 24. Until the actual shared window and live gates exist,
report **blocked on those conditions**, not running or completed.

1. Pull this integrated source before any run, verify the reproducible five-root
   release and use the [exact execution contract](e13a_execution_contract_20260923.md).
   Acknowledge this decision with real UTC, processed full SHA and planned run ID.
2. Seek the earliest feasible fresh slot through the established peer coordination
   route. Commit actual mutual acceptance, prior-job release evidence and the new
   launcher hash; inspect current host resources. No takeover based on silence,
   old reservations, an idle port or a second experiment worker.
3. Within that slot, initialize the single bound clock, renew nine containment
   checks on the actual grading host/interpreter, verify the attestation, launch
   one owned receiver, run the bounded non-generating preflight and snapshot diff,
   and commit actual PID ownership/evidence. The 600-second setup wall limit includes
   refreeze. A failed check or cutoff stops this attempt; no implicit retry.
4. Freeze the clean HEAD and dispatch the fixed 60 assignments. Preserve request
   strings, model/sampler/evaluator, selected checkpoints and every failure. Run
   the committed private grader and deterministic analysis; release the owned
   receiver and publish the actual exit observation and full accounting.

**Explicit maxima:** 60 generation attempts, 30,720 reserved completion tokens,
9 containment + 10 reference/control + 60 candidate starts = 79 total isolated starts;
0 public-score starts; setup metadata 50 and collection metadata 31 at most 10 seconds
each, no retry; setup 600s, collection 480s, private grading 300s, analysis 300s,
outer 2700s; one owned server launch; no installation/download, paid service or
extra task-frame review. The receipt must separate actual known tokens from
unknowns and private/control/containment starts. A resource/integrity failure may
leave partial evidence; it does not authorize replacement calls or reset budgets.

The worker's explicit guard-frequency question is answered: **retain preflight,
12 balanced-block state/idle checks and postflight**, with the source-enforced
31 metadata-attempt cap separate from setup's 50. These checks plus exclusive
ownership preserve the declared operational contract; they do not prove that no
transient undetected change occurred between checks. Do not add 60 new guard sweeps
or adjust checks using observed outcomes.

The former E12 instrument checks support unchanged assertion content only within
their finite scope. Fresh runtime containment and 10 reference/control rechecks are
still required; a source test is not a substitute. Existing E12 records, scores,
missed-public-defect example and negative conclusions remain immutable.

## Evidence and next milestone

E13a is a post-hoc-motivated prospective development follow-up on five fixed E12
public-fail histories: diagnostic-plus-instruction restart versus FRESH. It is not
an isolated diagnostic-effect experiment, a selected-policy validation, a
population trial or a demonstration of useful benefit. Report fixed-checkpoint
contrasts and all-assigned missing-outcome bounds; no population interval,
significance or six-draw futility conclusion. No primary point contrast is
available if any assigned primary outcome is missing. Preserve ties and negatives.

E13b as proposed, any final-public supplement, new generators and independent
policy collection remain outside MRL-20. The larger scientific milestones remain
matched fitted-history/final-precision validation, untouched policy evaluation,
and manuscript/reproducibility synthesis.

**58%, change 0 percentage points** under the unchanged fixed rubric. This repair
falls within existing harness/design credit and supplies no new efficacy or
independent-policy evidence. Full project not submission-ready. The next milestone
is the existing worker's accepted live slot and frozen bounded E13a delivery,
followed by independent reconciliation. Publish accepted/running/completed/blocked/
superseded with actual UTC, processed commit, run/freeze IDs and consumed resources;
publication of MRL-20 is not yet acknowledgement or execution.


## Peer receipt accepted and reservation committed — 2026-09-23T05:51:51.453010Z

The concurrent worker delivery `5d70a9b9ab7b31852f41c5f8289b808b02eb88c7` supplies the DTR-AgentEvals non-contention
agreement for **06:15–07:45 UTC on September 23**, with a worker availability check
at05:44:11UTC. The lead reviewed those published records and has not independently
observed the live host. This supersedes the earlier proposal-only status above.
The peer does not grant host authority; the owner's standing bounded-local-work
authorization and **MRL-20** supply the conditional one-run allowance. Use the
[accepted reservation](e13a_shared_window_20260923T061500Z.json), which pins the
repaired launcher and release. No additional lead approval is needed after the
stated live gates pass. Reconfirm availability and preserve any valid existing job.

Pull the repaired integrated HEAD before06:15, acknowledge its full SHA and the
planned unique run ID, then use the reservation as `$RESERVATION` in the exact
contract. At actual start initialize the clock; setup including fresh nine-check
containment, verified attestation, one owned launch, preflight/diff and ownership
refreeze must finish within600seconds. The06:45 readiness ceiling is an additional
outer window constraint, not permission to spend30minutes on setup. Do not copy
the prior placeholder ownership into the release. Commit real PID ownership
separately and preserve the actual execution freeze. Send the established peer
the actual PID at start and the release notice. The90-minute slot changes none
of60calls/30,720tokens/79starts or600/480/300/300/2700-second limits.

Window acceptance and authority are now published; MRL-20 acknowledgement, runtime
checks, actual execution and independent validation remain distinct pending states.
The worker's phrase “last blocker” does not establish that renewed attestation or
other live gates have passed. No new model/program execution occurred in this review.
