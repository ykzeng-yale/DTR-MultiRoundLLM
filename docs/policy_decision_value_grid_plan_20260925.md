# LEAD-POLICY-09 numerical decision-value grid: frozen plan

Frozen before numerical execution on 25 September 2026. **Purpose:** assess whether a smaller original-target independent-family trial could *in principle* cross the already proposed useful-gain decision boundary often enough to warrant a complete real-trial design. This is a deliberately stylized known-truth planning law, not a forecast of model performance, a policy fit, an empirical prompt effect, or a new inferential procedure. E14 remains NO-GO.

Use the [already reviewed two-contrast fallback](independent_prompt_policy_validation_design_20260922.md) with `g=0.05`, `r_n=sqrt(2 log(80)/n)`, useful benefit if observed family mean minus `r_n` is strictly greater than `g`, useful-gain futility if observed mean plus `r_n` is strictly less than `g`, and otherwise inconclusive. Evaluate hypothetical `n=198` and `n=544` **independent, equally weighted, complete, target-aligned families**. Neither count is a certified evaluation frame. Represent each family contrast by `D in {-1,0,+1}` with `Pr(D != 0)=q` and mean `delta`, so `Pr(+1)=(q+delta)/2`, `Pr(-1)=(q-delta)/2`, and `Pr(0)=1-q`. This law is a transparent paired binary-outcome difference scenario, not a claim that the real receiver follows it. Fixed grid: `q in {0.2,0.4,0.6}` and `delta in {0.05,0.10,0.20}`; all nine pairs are valid. Do not add or select scenarios after seeing the output.

Compute the exact finite-`n` distribution of the integer sum by dynamic-programming convolution, and report for all 18 cells the probabilities of useful benefit, useful-gain futility and inconclusive. Check that the probabilities sum to one, that calculated mean/variance match `n*delta` and `n*(q-delta^2)`, and that the algorithm matches brute-force enumeration for small `n`. No Monte Carlo draws, seed, fitting or outcome-driven interval tuning. Report the full grid, including unfavorable and nearly always inconclusive scenarios; do not turn it into a real-data power guarantee. This examines only `theta_1`; a real `theta_2` comparison, endpoint, family law, missingness, cost and joint policy claim still need a full freeze.

Execution cap: one local CPU process, 60 seconds wall-clock, under 256 MiB additional RAM, output under 100 KiB; zero model calls/tokens, benchmark/reference/candidate executions, network services and paid spend. The machine had approximately 23 GiB free disk at the preflight, and no matching active project model/benchmark process was seen. The script must run from this committed plan/source and write one new immutable JSON result. A real model stage remains held pending the complete protocol, versions/hashes, intervention support, root/family split, assignment, useful-gain/precision/futility, lease and numerical local cap.
