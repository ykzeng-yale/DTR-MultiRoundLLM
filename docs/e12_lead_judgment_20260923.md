# E12 independent scientific decision and E13 disposition

Coordinating scientific lead, 23 September 2026. **MRL-16 completed and independently reconciled; efficacy inconclusive. MRL-17 accepted as static description with interpretation corrections. E13a: amend, source preparation only. E13b: hold new stage.**

The worker delivered E12 at `1f65447`, MRL-17 at `dfa93c4`, the revised E13 proposal at `4817a02` and request plan at `5096dc3`. Latest status inspected is `22a0d22`. The approximately 20-hour publication-to-review delay was on the lead side. Queued heartbeat triggers were not completed reviews. The worker published and waited; no worker-silence escalation is warranted. This decision is published now, and its acknowledgement remains separate.

## Accepted evidence

E12 is fresh receiver interaction on 14 prospectively fixed **development** roots with adapted MBPP contracts, one public assertion and two private assertions per root. It is not independent policy validation or a population trial. It does not change the older E11 null or historical resampling comparisons.

Parent arithmetic and two independent reviewers checked the raw calls, assigned grades, analysis mapping, source/config bindings and saved execution records. All 33 indexed result files match their hashes; all 154 assigned call/grade slots reconcile, with zero missing grades. Nine published contrasts match recomputation. See the [outcome audit](../results/e12_outcome_lead_reconciliation_20260923.json), [execution audit](../results/e12_execution_lead_review_20260923.json), and [independent measurement review](../results/e12_measurement_independent_review_20260923.json). Reproduce the outcome arithmetic to a new path with `python3 scripts/reconcile_e12_20260923.py --output work/e12_review.json`; it executes no answer or collector.

| Frozen endpoint | Result |
|---|---|
| STOP | 10/14 private-suite passes |
| N0 and N1 | 20/28 each; every root mean equals STOP |
| S0, S1 and R1 | 19/28 each |
| Primary S1 minus N1 | −1/28 = −3.57 percentage points; one positive root, twelve ties, one negative root |
| S1 pass transitions relative to STOP | 1/8 fail-to-pass; 2/20 pass-to-fail |
| R1 pass transitions relative to STOP | 2/8 fail-to-pass; 3/20 pass-to-fail |

The primary strategy did not achieve an observed advantage. “Essentially null” is too strong: this is an observed negative contrast without a population interval or equivalence test. Two seeds per arm cannot establish whether the observed root differences predict future prompt benefit. No observed fixed arm improves on STOP; this is not proof that useful conditional effects are absent.

All 154 attempts used 53,670 prompt and 12,198 completion tokens, with zero unknown usage; 78,848 completion tokens were reserved. A+C collection time is 314.258 seconds, not A plus that cumulative total. B used 14 starts and D used 103 (28 reference/control rechecks plus 75 unique candidate starts), taking the cumulative ledger to **333/412**. Caching and pre-execution rejection explain the smaller execution count; all grade slots remain. Private grading took 2.531 seconds. Paid service cost was $0. Review arithmetic/AST work was below one second per pass; this is not the duration of the whole scientific review. The lead generated no new model calls/tokens, benchmark executions or simulated datasets.

The saved startup clock is 07:10:02.173407 UTC, ready at 07:10:03.180766 on September 22. Recorded guards show no drift. The saved stop timestamp is 07:15:30.254657, while `exited` remains null. The worker reports exit and release; the lead verifies that report and timestamp, not a completed-exit receipt or current host availability. A/C absolute boundaries and final-report time are not independently logged in their own artifacts. Retain available original exit evidence additively; do not reconstruct it or restart anything to prove the past. These reporting limits do not invalidate the reconciled outcomes.

## Measurement and mechanism corrections

On root 863 the initial program resets its run length at duplicate values, returning 2 for the public example whose correct answer is 3. Its private suite passes because the two private cases lack duplicates. The public diagnostic detected a real stated-domain defect; “private pass” is not complete program correctness.

Both S1 continuations on 863 receive zero under the frozen multi-block extraction rule, with **zero candidate execution starts**. One repeats the faulty function; the other retains the duplicate-handling error in its first block and is truncated later. This is not evidence that a successful public correction caused a semantic regression on private inputs. Preserve the scored pass-to-fail transitions, but distinguish them from demonstrated semantic damage. Do not rescue the negative by selecting a block or silently changing the scorer.

MRL-17's 140 static descriptions reproduce: S1 has seven unassessable outputs, all on public-fail roots; R1 has four. Function-definition AST identity is not semantic equivalence. The saved JSON wrongly labels itself “E11” and “7 reused roots”; its input paths and records actually cover E12's 14 roots. Preserve the original and annotate the metadata defect. The original public prompt already required one block or raw source and no prose. Repeating or strengthening that constraint is a new intervention, not restoration of an omitted requirement. An association between public failure and formatting failure does not identify mediation.

The analyzer's one unresolved family label and the manifest's 14 provisional duplicate-screen labels are different encodings; neither proves dependence or independence. No new family-level inference follows. The public/private cross-tab concerns initial answers only; final public compliance was not measured by this run.

## E13 ruling and next discriminating action

**MRL-18: proceed with one source/mock preparation bundle, at most 20 elapsed minutes, one CPU, $0, no model/receiver/benchmark-program/sandbox execution, installation or new task-frame review.** The existing worker owns it. Acknowledge accepted/running/completed/blocked with actual UTC, processed lead commit and package/run IDs. This allowance is not renewed by polling. Deliver partial work and the exact dependency at the cap.

Accept E13a's narrow scientific motivation: compare the diagnostic-plus-R1-instruction restart package against bare task resampling on the five fixed E12 public-fail checkpoints, six new continuations per arm. This is post-hoc-motivated development follow-up with fresh seeds, not a test of the selected rule or diagnostic-only effect. A tie is inconclusive, not proof of a resampling mechanism. Keep E12 outcomes separate; no pooling or seed replacement.

The single bundle must:

1. Correct the proposal's target, power and interpretation statements below; keep original proposals as dated history. Derive gating only from frozen public diagnostics and remove private STOP grades from executable inputs. Preserve the already checked R1/FRESH strings and noncolliding seed plan. Freeze root-balanced scheduling, inclusion probability one, model/evaluator versions, source/input hashes, missing and format rules, exact commands and immutable output paths. Keep changed formatting instructions outside this comparison.
2. Demonstrate the new two-arm, 60-artifact plus 10-control/recheck collection–grading–analysis path with source/mock checks. Do not reuse E12's hard-coded arm/count assumptions. Report all five per-root and equally weighted finite-checkpoint contrasts, all assigned failures and actual usage; no significance-driven escalation or futility claim from six draws.
3. State that the primary comparable endpoint remains the frozen private-suite-plus-format score. Specify a separately labeled final-public-compliance supplement and its additional execution cost if proposed; it is not free or a retrospective replacement for E12. Resolve the 863 limitation explicitly before any broader policy study. For this narrow follow-up, no change to private assertions is permitted.
4. Deliver one complete prospective execution contract: 60 receiver attempts, at most 30,720 reserved completion tokens, explicit collection/grading/outer time limits, runtime-attestation renewal procedure and all renewal/control/public-check starts, source bindings, actual shared-host availability and a proposed reservation. **333+70=403 is accounting, not authority:** E12 explicitly disallows spending unused starts on another study. Its attestation and window have expired. New execution remains held until this one complete bundle is reviewed and a separately enumerated budget and window are committed.
5. Supply retained original exit/release evidence if it exists; otherwise mark it unavailable. Correct future static-report metadata and annotate the E12 report without replacing immutable results. Do not rerun old grades.

**E13b is not released; do not spend the proposed 178-record review or 828 calls.** Its calculations test a zero null on the gated subset, not a five-point full-policy useful-gain null. Under identical STOP behavior outside the gate, overall gain equals gate frequency times conditional gain; at 5/14, a selected +.10 gated gain is only +.0357 overall, and +.05 overall would require +.14 gated. The five-root variance subtraction is a noisy moment estimate, not an identified decomposition. Gate-rate Wilson coverage is unsupported for this nonprobability roster; 124 retained and 44 gated are scenarios. The 3,506-family Hoeffding calculation belongs to one conservative precision procedure, not a universal minimum for validation. The Fisher examples do not justify declaring individual roots reliable after multiple comparisons. These are planning corrections, not new outcome evidence.

Independent mathematical review concurs with these corrections and this bounded preparation decision. STOP can be a named baseline in a prompting-and-stopping study, but replacing the fixed-continuation comparison changes the target; it cannot silently satisfy the original prompt-choice policy question. Independent policy evaluation still requires justified untouched families, frozen policies and fair information/cost comparators. Larger generator development remains held.

## Progress credit

**58%, +3 percentage points** from the last published 55% checkpoint, using unchanged weights. Fresh supported-prompt experiment completion rises from .20 to .40 of its 15-point weight (3 to 6 points) because the prospective 14-root batch is now completed and independently reconciled. This credit is for completing a bounded empirical milestone, including its negative result. Other component credits remain unchanged. Efficacy is unestablished; independent policy validation remains zero; the full project is not submission-ready.
