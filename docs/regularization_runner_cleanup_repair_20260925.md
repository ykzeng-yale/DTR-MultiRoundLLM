# LEAD-E0-RUNNER-02: preserve the final receipt across a process-group signal race

25 September 2026, 19:16 UTC. **Repair of the lead-owned bounded synthetic runner; no numerical study or model collection released.** Worker `a7261c5` accepted LEAD-POLICY-12 and reported an intermittent full-suite `PermissionError` at `os.killpg` in `scripts/run_regularization_comparison.py`. The reported command completed at 16:34:03 UTC with 1,663 passed and one failed; six isolated reruns and the runner file later passed. The exact failure is credible: the owned child can exit between `poll()` and group signaling, and macOS may return EPERM for its unreaped group leader. The earlier frozen E0 run and its outcome records are unchanged.

Both cleanup sites now call one bounded helper. It polls before signaling, tolerates a group-signal `OSError`, attempts a direct kill only if the owned child still appears live, waits within the existing deadline, and records the observed return code in the final receipt. A failed signal is never treated as evidence of exit: if the process cannot be reaped before the deadline, `child_exit_observed` remains false. This is an operational same-target repair, not a change to seeds, estimator, endpoint, resource cap or an archived run. A future execution would require a fresh reviewed source freeze and resource authorization.

Two deterministic fixtures force the EPERM branch: one live child is killed through the fallback and leaves a persisted final receipt; a child that exits between poll and signal is reaped without a redundant direct kill. The affected runner suite passed **26/26** (6.15 seconds). A full local suite completed in **32.36 seconds** with **1,654 passed, six failed and six skipped**; all six failures are outside the modified runner/test paths: two E14 committed-byte rebuild checks, one host-dependent ignored-log inventory check, two E2 absolute-path/target checks, and one candidate-frame committed-manifest check. They do not validate those components and are not silently marked green. The prior worker's one intermittent runner failure did not recur. No receiver/model calls, prompt/completion tokens, scientific datasets, E14 benchmark collection or paid spend resulted from this repair or these software fixtures; dollar cost $0.

The E14 NO-GO and independent-policy collection HOLD remain. Prompt efficacy is unestablished. Fixed-rubric completion stays **58%, change 0 points**; the full project is not submission-ready.
