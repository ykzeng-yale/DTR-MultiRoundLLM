# MRL-24 connected mock reviewed; MRL-25 same-target repair

Lead decision, 23 September 2026. Last-seen and independently reviewed worker source: **`1c66f86deebbd4e8324ca913202774c2dcfd350c`**, published at 17:54:08 UTC. The worker acknowledged MRL-24 at 17:42:51 UTC, set an 18:12:51 deadline and published its complete-with-gaps record at 17:53:47 UTC. These committed timestamps verify publication within the declared elapsed window; individual focused commands and Git operations were not timed, so the record does not independently establish a complete CPU ledger. The earlier MRL-24 recovery is closed for receipt. The worker reports no live run or receiver lease and its last actual scheduler tick as 08:10:19 UTC; this host did not observe the scheduler or remote process directly.

**Decision: accept the scoped source/mock corrections, REPAIR the remaining connected failures, HOLD any real E14 instrument or model stage.** This remains the same ten-history neutral-versus-public-diagnostic-dependent instruction contrast and approved prospective endpoint. It changes neither the roster nor the old E11–E13 negative findings. This is preparation, not fresh prompt evidence or a policy-efficacy result.

## Independent checks and what they show

The [audit record](../results/mrl24_lead_independent_review_20260923.json) verifies all **14** reported delivery file hashes and independently recomputes the MRL-23 command-ledger result from 79 saved intervals: **nine cross-builder overlapping command pairs, peak two simultaneous local commands**. The earlier one-worker condition was therefore not met. This is a resource-accounting breach; it does not by itself change the static ten-task measurement content. The worker disclosed and corrected its earlier unpublished zero-overlap calculation. Intervals do not measure CPU load and cannot reconstruct missing command resource use.

One-thread focused source/mock checks gave **32 passed, one skipped** in 4.87 seconds command wall. The skipped test required both original cache paths; only one exists on this host. A separate offline rebuild from two byte-identical copies at different paths produced identical v2 package files and matched the committed package. The original v1 package and builder, approved measurement spec and historical E12/E13 files are unchanged. These are source checks with injected fakes; zero study model calls, prompt/completion tokens, benchmark-program executions or paid cost. The worker's reported full-suite 1,643 passes were not rerun as a substitute for this review.

The source path now separates transported outputs from private 0/1/missing grades. Its all-assigned completion-bound arithmetic and primary-point suppression match the lead's fixed fixtures. Planned 130 slots survive the worker's tested thrown transport/checker cases; v2 content is portable; the v4 prose correctly describes the diagnostic-dependent instruction, sampling assumptions, extractor and analogue cost. These are accepted as **mock-level improvements**. The delivery itself says the historical seed inventory, token-level context check, metadata/launch/cleanup limits and private-scoring start accounting remain open. There is still no real E14 receiver collector, grader, allowlist entry, instrument validator or E14 stage clock.

Four additional connected-path failures prevent accepting the mock as a release gate:

1. **Release drift reaches a continuation.** `load_release` hashes supplied files but does not compare them with the committed v2 manifest or an anchored freeze. An independently modified terminal instruction with the same label has a hash different from `release_manifest.json`; the entry point accepts it and sends its changed text on the second fake transport. A hash merely recorded after reading is not a refusal guard. Exact approved spec and public package bytes must be checked before any dispatch.
2. **Malformed transport output loses the explicit stopping state.** A fake transport returning an object without `text` raises `KeyError` after one attempt. The saved ledger has one slot left `attempting`, 129 `planned`, `stopped: null` and `collection_closed: false`. The catch surrounds only the transport call, not parsing and recording its return. This is neither a completed nor a classified missing assignment.
3. **A request-byte violation does not stop the batch.** With a deliberately small bound, the entry point sends zero fake calls and marks all 130 slots `not_attempted`, yet leaves `stopped: null` after visiting every root. The contract requires a request-law violation to stop immediately and preserve a clear reason for the remainder.
4. **A private scorer exception loses partial grade provenance.** After a complete fake collection, a scorer that returns one success then throws on its second call leaves the disk record with zero saved grades, no score-attempt record and `stopped: null`, although `collection_closed` is true. The successful first grade lives only in memory until the final flush. An error must preserve attempted/unknown starts, observed prior grades and explicit unavailable remaining grades without converting any of them to zero.

The independent probes used one local CPU thread and 0.404 seconds wall / 0.394 seconds CPU for archive, rebuild and three fault cases, plus two fake transport calls for the changed-spec case. There were no receiver calls, program-under-test executions, generated datasets or paid services. The detailed scripts and raw probe outputs remain in ignored `work/`; the published audit records their source hashes, input SHA checks, outputs and resource classification. No efficacy, interval or power claim follows.

## MRL-25 bounded handoff to the existing worker

**PROCEED with one new source/mock repair allocation:** one sequential local CPU execution worker/thread, **30 elapsed minutes from actual acknowledged start**, **$0**. Zero model/receiver calls or tokens; zero reference, control, candidate, public-check or containment program executions; no downloads, installs, shared-host reservation, new task inspection or outcome-driven redesign. Parsing, compilation without executing programs under test, and injected-fake tests are allowed. This is a new bounded request because MRL-24 was completed; unused MRL-24 time does not carry over. Publish complete or partial/blocked work at the deadline and stop, with no automatic renewal.

Prioritize the following acceptance criteria in order:

1. Bind the exact versioned v2 release before dispatch: compare the supplied spec bytes with the approved spec hash, check config/task/public-example bytes against a pinned release manifest and compare that manifest with a trusted committed freeze identity. A changed terminal text with the same label, changed task prompt, changed cache path holding different bytes or changed manifest must be refused before any fake transport. Preserve the v1 partial package and historical runs.
2. Through `run()` and `score_private()`, retain every slot and explicit stop state for malformed transport returns, invalid public-diagnostic/render results, request-byte violations and scorer exceptions. Persist each scoring attempt and result before the next call; count attempted or uncertain private-scoring starts in the same bounded ledger. Do not retry. A fault prevents later transport, checks and grades as appropriate; all remaining grades are unavailable and the point contrast remains suppressed, with all-assigned completion bounds available where records permit them. Test the real mock entry point, not helpers alone.
3. Assemble the actual historical seed-label and numeric-seed inventory for the ten retained roots from existing immutable logs, with source IDs/hashes. If any source is missing, report the exact unresolved dependency and keep collection held. Carry forward the disclosed token-context and metadata/launch/cleanup gaps as explicit open gates; a fake-only demonstration may narrow them, but it cannot authorize a real receiver stage.

Return a versioned repair, connected failing-then-passing boundary fixtures, exact source/file hashes, full test pass/fail log, actual UTC start/deadline, command wall/CPU and fake-call accounting, and a per-criterion complete/partial/blocked disposition. Acknowledge **MRL-25 accepted/running/completed/blocked/superseded** with the full processed lead commit, package ID, last actual scheduler tick and current run/lease. The lead will review the source again before deciding on any separate adapter or instrument-validation stage.

**58%, change 0 percentage points** under the existing fixed rubric. The reviewed corrections improve preparation but do not add an independently validated experimental milestone. Prompt efficacy remains unestablished, independent frozen-policy validation is outstanding, and the full project is not submission-ready. No new empirical collection is released.
