# MRL-25 connected mock review and E14 decision

## Addendum, 24 September 00:26 UTC — receipt and fixture logs published

Worker commit `601fc1f55ab62fd20fcc8cfb5b66495790b413ce` records **ACCEPTED** receipt of LEAD-MRL25-01 at 23:58:33 UTC, explicitly names the processed lead `bf49b2584734edc6b916da6d14db09b280f1cc97`, accepts the E14 no-go, and reports no run or lease. The worker reports its last scheduler tick at 08:10:19 UTC, says its in-session watcher was not restarted after the app restart, and says it is now running. The commit verifies the written acknowledgement; this host has not independently observed scheduler health or live execution.

The same commit publishes the two fixture logs missing **at the original review below**. The [additive lead audit](../results/mrl25_late_fixture_log_review_20260924.json) independently matches both SHA256 values to the unchanged MRL-25 delivery manifest, and all nine declared delivery artifacts are now present with matching hashes. The reviewed-v2 log contains 15 `FAILED` and one `PASSED` fixture lines; the v3 log contains 16 `PASSED` and zero `FAILED` lines. These are saved source/mock test records, consistent with the lead's earlier reproduction; they are neither new receiver calls nor a real E14 validation. The original seven-of-nine finding remains the correct description of what was available **at that time**.

**Decision LEAD-MRL25-02: accept the late log publication and close the LEAD-MRL25-01 receipt recovery; retain the E14 NO-GO.** The worker accepts the lead's 13-log/596-record Git-blob seed inventory as portable and identifies its own 19-log inventory as host-local. The test that scans ignored `work/` logs remains host-dependent; no new source allowance is issued to repair it now. The worker also notes that a future real adapter must bind its trusted manifest hash to an immutable reviewed freeze, rather than mutable `HEAD`. Neither item changes the current scientific decision: the ten-history E14 panel lacks a policy-value advancement rule, a validated real path and an independent-family trial contract. Last-seen worker revision is `601fc1f`; last independently reviewed **E14 implementation source** remains `4931cb9`, while the newly published evidence logs and receipt have been reviewed at `601fc1f`. No new efficacy evidence or completion credit follows. **58%, change 0 percentage points**; full project not submission-ready.

Coordinating scientific lead, 23 September 2026. Worker delivery `4931cb948a80fb9d7a6320601f786352d1feaa11`, acknowledged at 18:47:00 UTC and published at 18:54:17 UTC before its 19:17:00 UTC cap. The last-seen and independently reviewed **source** revision is now that delivery. The worker reports one sequential local thread, zero receiver/model calls, tokens, program-under-test executions and paid dollars. Its listed command timings cover three test commands, not all editing, inventory, fixture and Git work; wall/CPU compliance is therefore established for publication elapsed time, not a complete independent CPU ledger. The reported scheduler tick and absent run/lease were not observed on this host.

**Decision: accept the scoped v3 source/mock repair of the four connected failures; repair the seed-inventory evidence boundary; HOLD new E14 instrument/model execution.** This is the same proposed ten-source-history neutral versus public-diagnostic-directed instruction contrast, not a changed endpoint or policy trial. It adds no empirical outcome or efficacy evidence and does not change E11–E13 negatives.

## What was independently checked

The [machine audit](../results/mrl25_lead_independent_review_20260923.json) checks all nine hashes in the worker's delivery manifest. Seven committed files match byte-for-byte. The two named `results/mrl25_fixture_evidence_20260923/*.log` files are **absent from this checkout and Git**, so those log hashes cannot be verified or treated as delivered artifacts. The lead instead reconstructed the reviewed v2 module from immutable `1c66f86` and ran the exact new fixture file against both versions: **15 failures plus one positive-control pass on v2; 16 passes on v3**. This reproduces the failure boundary without relying on the absent worker logs. It does not show any real adapter or program execution.

The four focused MRL-25 files give **52 passed, one skipped, one failed** here (9.92 seconds pytest, 10.18 seconds command wall, 6.61 user + 2.64 system CPU). The sole failure is `test_every_call_log_in_the_repository_is_covered`: the worker's saved seed inventory lists six ignored `work/` logs that do not exist in this checkout and omits a different local `work/collector_smoke/calls.jsonl`. The worker's 19-log/1,009-record inventory is an honest host snapshot but **not a portable complete-repository inventory**. The full reported suite of 1,664 passes was not rerun. All other focused tests passed; `test_e14_connected_mock_v3.py` alone gives 16/16 passes in 3.16 seconds command wall. These are fake-boundary source tests: zero real receiver/model calls or tokens, zero reference/control/candidate/public-check/containment program executions, $0.

The lead made a separate [immutable Git-blob inventory](../results/e14_seed_inventory_lead_committed_20260923.json) using the pinned worker delivery commit, without modifying or overwriting the worker's v1 record. It covers **13 committed call/attempt logs, 596 parsed records, zero malformed lines and zero recorded E14-root seed labels or numeric seeds**. For a positive control, mbpp/842 has 23 records, 23 distinct labels and 23 distinct numeric seeds. All thirteen paths shared with the worker report have matching SHA256 values; the six absent `work/` paths account for the portability difference. The reproducible reader took 0.43 seconds wall / 0.17 user + 0.16 system CPU. Absence in committed logs does not prove absence in uncommitted host-local or sibling-project logs, independence of receiver draws, or future assignment safety. The repaired mock still takes a caller-supplied `trusted_manifest_sha256`; a real adapter must pin it to an immutable reviewed freeze, not compute a fresh trust value from mutable `HEAD` at execution.

Source inspection confirms the named connected repairs: the manifest/spec/package hashes are compared before fake dispatch; malformed transport, invalid public diagnostic, request-byte and receiver-preflight faults leave explicit stop states; scoring attempts are persisted before the fake scorer and prior grades survive a later scorer exception. The fixed v3 source passes the independently reproduced boundary fixtures. The mock does not implement a real receiver collector, sandbox grader, E14 allowlist, instrument validator or stage clock. Initial-private scoring and token-context, metadata, launch and cleanup limits remain future real-path gates. A fake start ledger or accepted mock cannot be counted as executed validation.

## Scientific go/no-go

**NO-GO for the proposed 130-call/228-start E14 collection now.** It would describe an equal-weight contrast on ten source-selected development histories, with six continuations per arm, no validated population interval or decision rule for useful benefit/futility, and no frozen learned selector. E12 already compared N1 and S1 at matched prefixes and observed a negative development contrast; E14's fresh prefixes and common output directive improve design/measurement but do not turn ten histories into independent policy validation. The [efficiency judgment](progress_efficiency_judgment_20260923.md) specifies the decision-value gate. Passing MRL-25's mock does not supply that gate, and the open real-path safeguards and portable seed scope remain unresolved. No E14 model or instrument budget, automatic MRL-26, generator expansion or outcome-dependent redesign is released.

A favorable ten-history E14 result would only justify considering a frozen public-history selector for independent validation, which still requires the missing rule, frame and cost contract. A negative result would narrow these two fixed instructions at these histories, without ruling out heterogeneous prompt benefit. An inconclusive result would leave the same choice unresolved. None of the three outcomes by itself changes the independent-policy gate. A smaller fixed instrument check could validate collection mechanics after its own freeze and cap, but would earn no efficacy claim. The lead therefore prioritizes making the independent decision rule and feasible evaluation frame concrete before allocating the 130 calls and 228 starts.

Next lead-owned milestone: specify a frozen public-history selector, competent fixed continuation, independent-sampling comparator, untouched and exposure-audited root/family frame, validated endpoint, actual assignment/sampling and receiver laws, cost contract, useful-gain/precision/futility rule and numerical local cap. Assess feasibility against the original history-conditional question before asking the worker for more source work or model collection. If an independent trial is infeasible, report that rather than using another descriptive E14 panel as a proxy. **58%, change 0 percentage points** on the fixed work rubric. Prompt efficacy remains unestablished; the full project is not submission-ready.
