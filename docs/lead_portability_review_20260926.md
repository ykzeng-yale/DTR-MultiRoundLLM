# LEAD-PORT-01 independent review and LEAD-PORT-02 repair boundary

26 September 2026, 01:16 UTC. **Decision: accept the clean-checkout reporting repair, but repair the remaining populated-host portability tests; hold all model collection.** This is source/test reproducibility evidence, not a new endpoint or efficacy result. The worker acknowledged lead `7c5ed08` at 22:16:25 UTC and delivered source repair `82b9ff7` plus receipt `774dfa0` at 22:21:33 UTC. Its clean-worktree logs report 1,625 passed, 6 failed, 7 errors and 28 skipped before, then 1,634 passed, 0 failed/errors and 39 explicit skips after. The skipped rebuilds do **not** verify the committed packages. The worker reports one sequential CPU test worker, zero model/receiver calls, downloads and paid spend. Its three retained logs total 33,893 bytes; including `summary.json`, the new evidence directory totals 37,538 bytes, still below the 256 MiB allowance. The 920 pre-existing tracked `experiments/` and `results/` files were unchanged by the repair; only tests, source-acquisition documentation and new evidence were added.

On the lead's populated host I independently ran the five affected test modules with `uv run --locked --no-sync python -m pytest -q`: **94 passed, 3 failed, 1 skipped in 1.40 s**. The E14 v1 deterministic rebuild and committed-package `verify` fail solely because the builder's `CHANGE_NOTES.md` and `release_manifest.json` embed `work/task_sources/mbpp_full_20260920_4700efb9/mbpp.jsonl` instead of the committed package's `work/sources/mbpp_full.jsonl`; both source locations contain bytes with the same required SHA256 `ccf64ceae9c5403bf50a044cb6d505bfd2a2963ee58338ba268fd65beab92a9f`. The E2 fixture byte test differs solely in `inputs.e2_base_interpreter_prefix`: the recorded execution host used a uv Python 3.12 path, while the current host resolves Homebrew Python 3.14. Its E2 items, code and other inputs were semantically identical in the rebuilt JSON under the test's recorded-path projection. The worker's populated-host 1,673-pass report is a separate host observation; it does not establish portability on this host. These failures are metadata/path binding, not evidence of changed private checks or model outcomes.

**LEAD-PORT-02, existing experiments worker only:** a new, source-only allowance to make the three tests state and check the correct two contracts: committed-byte reproduction only under the committed source/interpreter binding, and an explicit portable projection/check under an alternate verified source location or interpreter. Preserve the frozen E14 package, E2 executed fixture, builders' source-SHA fail-closed behavior, and all old evidence. Do not weaken a byte-equality claim into an unlabeled semantic comparison, claim a skip as verification, download data, or run a benchmark/model. Show the current-host mismatch before and affected tests passing or explicitly constrained after, with any remaining host dependence visible. One sequential CPU test worker, 10 minutes wall from acknowledged start, at most 64 MiB newly retained output, zero model/receiver calls, zero paid spend and no shared receiver lease. If the exact bound cannot be met, return blocked with the smallest unresolved test and reason. Acknowledge accepted/running/completed/blocked/superseded with real UTC, processed lead SHA and final commit/run IDs. The lead will review the resulting test semantics and hashes before accepting cross-host reproducibility.

Scientific boundary remains E14 N1/S1 **NO-GO** as a next efficacy stage. A supported history-conditional same-prefix prompt selector, fixed comparator, untouched independent-family frame, endpoint, assignment probabilities, inference and full local collection cap remain unfrozen. Prompt efficacy is unestablished; independent policy validation is absent; full project is not submission-ready. Fixed-rubric milestone completion remains **58%, change 0 percentage points**.
