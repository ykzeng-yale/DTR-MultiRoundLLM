# MRL-16: bounded preparation and conditional E12 execution

Lead decision, 22 September 2026. MRL-15 grader v5 and its 31-start instrument check are accepted in their finite validated scope. E11's primary null result remains valid descriptive evidence. Its result is not “nothing,” and there is no basis for calling E12 the smallest study able to observe repair.

This release bundles source preparation, new-contract validation and one fresh development run. Passing the explicit gates below does not require another half-hour lead approval. A violated gate stops only its dependent stage; report the exact blocker and completed independent work. No reruns or replacement-root searches are included.

After the delivered 42-start validation, apply the [accepted validation and explicit scheduling amendment](e12_validation_acceptance_20260922.md): record precollection scheduling wait separately, charge validation at least one minute, and finish A through the final deterministic report within the remaining execution allowance (at most 59 contiguous minutes). All call, token, start, phase-time and ownership limits below remain. This amendment also supplies the final report invocation omitted from the manifest's input-preparation command E.

## Scientific target and roster

The cohort is a fixed prospective development roster from the MRL-15 source frame, with the source-based exclusions and public-contract clarifications in `docs/e12_contract_review_20260922.md`. The retained14IDs are **918,825,842,816,895,868,288,154,863,966,652,651,499,974**, in original hash order, without backfill. Hold31,847,907,963 in addition to the earlier359/349 holds. Adopt the exact public clarifications in the review, including the explicit general-modulus amendment for288; these are versioned adapted MBPP contracts. The mechanical screen is not semantic clearance. Hold any additional contract conflict before model dispatch and publish it; do not improvise a different endpoint or import private expected answers into prompts.

Family representatives were selected by minimum member hash and ordered by that minimum. This is not uniform family sampling. Retain the roster and report finite-roster descriptive results; withdraw uniform-family, representative-population and independent-family claims. `docs/e12_independent_design_review_20260922.md` records the independent review. No population confidence intervals, HT weights or significance tests are authorized here.

All retained roots receive STOP plus N0, S0, N1, S1 and R1, two continuations per active arm. Primary contrast is S1 minus N1 with identical diagnostic bytes; retain N1–N0, S0–N0, R1–N1 and every STOP comparison. Report every root, including format failures and unavailable outcomes. Log the actual existing `arm:replicate` seed derivation and do not call it common-random-number coupling. Every root-arm is included by design (inclusion1); randomized scheduling is not treatment selection with propensity1/5.

The proposed one-public-case/two-private-case convention changes the information and measurement package from E11. Label it a new development contract, not a replication with identical endpoint. Actual public/private call-input overlap must be checked, not assumed from different list positions. Preserve private expected answers. No authored boundary tests, benchmark-wide correctness or independent learned-policy validation is claimed. A high STOP rate remains a valid result; do not drop successful roots or stop/tune based on observed quality.

## One-time resource authority

Preparation: one CPU worker, at most45 wall-clock minutes, source/mock/static work, no model/receiver/sandbox execution or installation. Deliver partial work at the cap if needed.

Conditional validation and execution: one local worker, no new service or hardware purchase, $0 paid APIs, no installs; at most60 wall-clock minutes for validation through analysis. Use the existing pinned receiver only during a fresh, real exclusive-use agreement, with at least60 minutes available when collection starts. The old agreement ending02:09:48Z is expired. Preserve nonblocking status publication while a scheduling reply is pending; do not infer consent from idleness or interrupt another job.

For N=14 retained roots (reduce counts for any pre-dispatch source hold; never backfill):

| Component | Worst-case allowed count |
|---|---:|
| Before collection: private reference + one negative control | 2N starts |
| Before collection: public reference through exact public checker | N starts |
| Phase B: initial public diagnostics | N starts |
| Phase D: initial +10 continuation grades, plus separate reference/control rechecks | 13N starts |
| Total additional isolated starts | 17N≤238 |
| Receiver calls including failed attempts | 11N≤154 |
| Reserved completion tokens, at512 per call | 5,632N≤78,848 |

The proposed252-start count omitted separate precollection validation versus grading rechecks. This release explicitly adds a new budget:174 starts already spent plus at most238 new starts, cumulative ceiling412. Unused budget is not a retry or unrelated-experiment allowance. Collection A+C remains capped at1,200 seconds; private grading at600 seconds; the outer60-minute cap still governs. Prechecks, failures, controls and actual launches are charged. Cache savings reduce actual usage only; do not assume them in ceilings. If the attestation expires or its host/runtime binding changes, stop and report; no refresh allowance is implicit.

## Committed gates before dispatch

1. Build and commit a separate `dev_release_v3` package with exact retained roots, exclusions, public prompts/interfaces/domains, one public example and the declared private assertions, one meaningful source-designed wrong control per root, replacing the do-nothing stub rather than adding starts, prior-exposure list, provisional families, model/evaluator/runtime versions, seed schedule, deterministic assignment table and all source/config/task/spec/contract digests. Preserve v2/v2.1 and E11. Primary reporting stays descriptive; the future5pp usefulness target is not a confirmatory decision threshold here. Use a fixed batch and stop only for integrity/resource failures, not interim efficacy/futility.
2. Repair the old-package assumptions together: the grading CLI currently permits only `dev_release_v2/release_manifest.json`, hard-codes77 artifact and24 recheck starts, and the adapter rejects more than200 private starts. The retained14-root private phase needs182starts, so preserve that200-start ceiling; raise the explicit grading-time cap to600seconds as authorized. Replace the old path and77/24 assumptions with strictly verified committed v3 bindings and explicit manifest limits of11N artifact and2N recheck starts capped by this release. Support v3 public examples and arbitrary retained roster size without mutating v2. Do not broadly accept arbitrary untracked manifests. Fake-test exact A–E paths, N-root counts and limits, failed/missing outputs, interrupted writes, wrong package/hash and resource-fault preservation. Freeze all changed source bytes before actual starts.
3. Before any receiver call, run the exact3N new-contract validation starts on the unchanged, unexpired attested worker runtime: every private reference passes, each control fails the **private** suite, and every public reference passes. Record zero-start static rejections separately; they do not satisfy an executed control-discrimination gate. If a mandatory check fails, preserve evidence, hold collection and return the concrete root/check; do not quietly drop it and continue.
4. Commit exact A–E commands and the run's fixed output location; publish accepted/running with UTC, processed lead SHA, frozen HEAD, run ID, current shared-hardware agreement and cap ledger. The same HEAD must govern A–E, with checks before every phase; no pull or mutation mid-batch. Live receiver law/slots and ownership checks remain mandatory. Diagnostics bind to the actual initial artifact and public-example bytes; the analysis uses recorded Phase B costs.
5. Publish full or partial immutable results by the cap, including calls, known and unknown tokens, phase times, executor starts/results, private controls, source and artifact hashes, receiver checks, assigned/completed/missing counts and all prespecified root contrasts. Keep grading cost separate from decision-time cost. Preserve failed and unavailable records. No new generator or learned-policy trial is released.

## Reporting

Planning completion remains55%, delta0. The grader repair and frame review remove concrete barriers, but they are not fresh E12 outcomes or positive efficacy. Next deliverable is the committed package and its validation/collection evidence, or a precise failed gate—not another unbounded proposal.
