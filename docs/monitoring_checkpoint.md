# Hourly research checkpoint

Updated 20 September 2026. This file records scientific state for the hourly continuation; it is not a substitute for inspecting live GitHub before every run.

## Schedule and reviewed state

- Active thread heartbeat: `dtr-multiroundllm-hourly-research-follow-up`, once per hour. Its scope is this repository, distinct from the existing DTR-AgentEvals routing automation. Continue useful bounded work and notify on meaningful progress/failure/decisions; do not post repetitive no-change comments.
- Last external state inspected: main `17215b54cf2dd2d6b8a5c7759bd6479bdec5c6f3`, PR #6 open at attribution-corrected `a57a9d1c2069fc7342d0fa9d9ee9d198e5ff2527`. No new external experiment result or work-package acknowledgement since the preceding handoff was observed. The subsequent continuation changes are published on PR #6's same branch; inspect the live head rather than treating the recorded parent as current forever.
- Commit identity: Yukang Zeng <ykzeng2019@gmail.com>. The owner-authorized historical attribution correction is complete; do not restore old ancestry.

## Scientific progress in this continuation

The landmark theory now separates marginal effects, public-history personalization, checkpoint information, and noisy branch selection. It supplies scoped identification, regret, root-variance and transport arguments with seven exact check groups. The new known-truth study has 1,200 independent simulated datasets and 2,400 nested evaluations, not 2,400 independent datasets. It shows why a zero average effect is not a personalization futility criterion and why the best observed branch is not an achievable oracle. No new real-model efficacy evidence was obtained.

A bounded collector/offline analyzer and a reviewable next-experiment protocol are delivered with a two-root, 14-mocked-call run and 82 passing project tests. No mock result is an empirical quality score. The collector's wording adaptation is a frozen syntactic scaffold, not a learned semantic prompt policy. The real-run release must still bind task/evaluator/source/model contracts and pass live environment checks. Existing source labels are full-benchmark grades, not the later hidden-only endpoint.

## Next scientific work, in priority order

1. Review and integrate PR #6's scientific corrections and new landmark work; resolve any reviewer findings against the declared estimand. Do not repeat completed synthetic runs unless a change or discrepancy warrants it.
2. Audit a prospective root/family task source against all 591 previously used canonical tasks and available project logs. A different 561-row file is not automatically a new cohort. The completed task-source audit identifies 544 additional MBPP IDs after known prior-description exclusions (242 official-test IDs); candidate IDs do not certify freshness, independence, or a correct evaluator.
3. Complete the public-information/private-grade adapter, reference/negative-control and sandbox tests, and pinned receiver/service contract. This host currently has no reachable receiver service at the inspected ports and only verified 0.5B/1.5B Ollama artifacts, not the historical 3B/7B runtime. New receiver choice must be explicit.
4. Freeze a bounded development batch only after those checks. The protocol ceiling is 24 roots, two continuations per non-STOP arm, 168 calls, 512 completion tokens per call, 20 minutes, $0. Current strict exclusion mode cannot silently reuse the old benchmark; a reused-development extension would require an explicit manifest mode and scope.
5. Learn/tune on development roots, then evaluate a frozen selector and competent comparator on untouched roots/families with justified useful-gain/precision rules. Only then consider generator expansion or a separately declared compute-efficiency application.

## Readiness and reporting boundary

Full project: **not submission-ready**, unchanged in that categorical status. The present advance is design/theory/software validity; no claimed efficacy or safety certificate has been added. The largest remaining milestones are validated measurement/receiver/task freeze, actual supported prompt-intervention data, independent policy validation and manuscript integration. Historical readiness percentages are subjective planning scores and are not probabilities of scientific success. Report new negative, inconclusive and positive evidence equally and retain the coordinating agent's responsibility for choosing the next discriminating test.
